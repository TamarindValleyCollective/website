# Change Log — August 2026

One line per change: date, title, requester, the *intent* behind the ask (not a verbatim
quote), what actually changed, and the commit hash(es). Newest at the top. See `CHANGELOG.md`
for the format note and index of other months.

- **2026-08-01** Require dev-server verification before closing (contact) — make sure changes
  are actually rendered and checked with the user, not just built; added a requirement to the
  "Development" section of `AGENTS.md`/`CLAUDE.md` to always render in the dev server and
  verify before closing it. `7e82ca9`
