# Change Log — August 2026

One line per change: date, title, requester, the *intent* behind the ask (not a verbatim
quote), what actually changed, and the commit hash(es). Newest at the top. See `CHANGELOG.md`
for the format note and index of other months.

- **2026-08-19** Complete new WABA phone verification, surface missing payment method (Sharath) —
  resumed the WhatsApp setup after the OTP rate-limit cooldown; Sharath answered the verification
  call and confirmed the code, moving the new WABA's number from Unverified to Pending. Checked
  WhatsApp Manager and Business Settings directly to confirm the new WABA's ID (`2166888700553540`),
  its Phone Number ID (`1252670697930284`), and that "Tamarind Valley Collective" display name is
  Approved on it too. Found the new WABA has no payment method attached yet (expected, since
  ditching AiSensy's shared credit line was the point) and the System User isn't assigned to it
  yet — both now open checklist items, along with redoing the permanent access token against the
  new WABA. `33d7389`

- **2026-08-19** Pivot WhatsApp WABA off AiSensy billing, start migrating the number (Sharath) —
  found the WABA's only payment method was a credit line billed to AiSensy directly (not TVC),
  with no self-service way to detach it (tried finance-editor role, add payment method, credit
  line removal — all blocked); Sharath asked to delink from AiSensy entirely, so decided to
  abandon that WABA and create a fresh TVC-owned one, migrating `+91 80 4110 9754` into it.
  Along the way: enabled 2FA for both business admins (unblocked System User creation, which had
  been failing with a generic error), created System User "apiuser" and started a permanent
  access token request (pending peer approval from Rajesh). Number migration is in progress,
  blocked on an OTP rate limit (landline can't receive SMS) until it clears. Also added a
  Phase 4 checklist item for building a reply UI, since Meta's Cloud API has no inbox of its own.
  `d80d146`

- **2026-08-19** Update `WHATSAPP.md` with production number, display-name approval, and OTP
  status (Sharath) — Sharath reported the WhatsApp display name got approved; checked WhatsApp
  Manager directly and found the production number changed to `+91 80 4110 9754` (Phone Number
  ID `1241370129064258`), replacing the earlier unrecognized `+91 80 6252 4957` placeholder,
  with "Tamarind Valley Collective" now Approved as its display name; closed the open "which
  number" decision. Follow-up check of the Meta app's Production setup wizard (the
  authoritative source, not WhatsApp Manager's own signals) found the number is *not* yet
  registered for API sending — flagged as the next step, blocked on Sharath receiving/entering
  the SMS/voice OTP. `f78c417`, `514dbd0`

- **2026-08-19** Add `UPI-SYNC.md` documenting the BHIM UPI transaction-sync automation
  (Sharath) — asked to commit the doc for the Apps Script project (unrelated to the website
  itself) that polls a Drive folder for BHIM PDFs and syncs rows into the Funds Transfer
  Requests sheet, replacing an earlier local launchd/Python version; kept alongside
  `RAZORPAY.md`/`BUFFER.md` since that's where TVC's other operational tooling docs live.
  `9aabf23`

- **2026-08-19** Scaffold Matt Pocock agent-skills config: GitHub issue tracker, default triage
  labels, single-context domain docs (Sharath) — ran `/setup-matt-pocock-skills` to wire up
  per-repo config the engineering skills (`triage`, `to-tickets`, `to-spec`, etc.) assume; added
  `docs/agents/issue-tracker.md`, `docs/agents/triage-labels.md`, `docs/agents/domain.md` and a
  new `## Agent skills` section in `AGENTS.md` pointing to them. `4ea1a4e`

- **2026-08-19** Document WhatsApp integration plan in `WHATSAPP.md` (Sharath) — asked what's
  possible for a WhatsApp integration; audited the existing AISensy account (Meta application
  still pending, one usable approved template, real webhooks/API gated behind the ₹3,040/mo PRO
  plan) and decided to integrate directly against Meta's free Cloud API instead. New
  `WHATSAPP.md` records the audit and a four-phase setup checklist for the account-owner-only
  steps (Meta app, business verification, phone number + token) ahead of building the webhook
  function. `1da0faa`

- **2026-08-19** Automate the member-profile-update notification email (Sharath) — wanted the
  members@tvc.farm heads-up (member name, what changed, photo, direct link, "N of 53 members
  have shared their story" stat) sent automatically instead of by hand each batch; new GitHub
  Action (`member-update-email.yml` + `send-member-update-email.mjs`) fires on every push to
  main touching `members.ts`, diffs it to find changed cards, and sends via Resend — set up the
  `tvc.farm` sending domain (DKIM/SPF/DMARC in Cloudflare) and API key this session, plus a
  bulletproof-HTML rewrite after the first real test send showed the original flexbox/`<style>`
  version breaking in Gmail. `2febf88`, `0063710`, `d67d847`

- **2026-08-19** Process 3 member profile updates (Sharath) — checked for new "TVC Members: Your
  Story" submissions; populated Nirmal & Shalini and Harishri Babuji's cards (bio, family,
  whyTVC, photo) from their first-time responses, and applied Ram & Krishna's follow-up edit to
  their already-live card. `0355a3c`

- **2026-08-19** Close the remaining image-size Dependabot alerts via @netlify/blobs (Sharath) —
  asked to look into the 2 open high-severity alerts (CVE-2025-71329/71330, DoS in image-size's
  ICNS/JXL/HEIF parsers) left over from the 2026-08-16 pass; image-size itself has no patched
  release, but `@netlify/dev-utils` dropped it entirely as of 5.0.0, bundled starting with
  `@netlify/blobs` 10.7.13. `npm update @netlify/blobs` picked that up within the existing
  package.json `^10.7.10` range — no major bump — removing image-size from the tree outright.
  `npm audit`: 0 vulnerabilities. `72e2aee`

- **2026-08-19** Fix dev-mode Pagefind stub crashing every page (Sharath) — asked to polish the
  homepage, but `astro dev` was throwing a fatal "failed to resolve import" overlay on every page
  load; the 2026-08-13 dev-only Pagefind stub's `/* @vite-ignore */` comment stopped suppressing
  static import resolution after a Vite 8 upgrade. Held the import path in a variable instead —
  Vite never statically analyzes a non-literal specifier, so no ignore comment is needed.
  Production build unaffected. `c7c61eb`

- **2026-08-19** Update the Impeccable design skill to v4.1.1 (Sharath) — ran `npx impeccable
  update`; syncs `.claude/skills/impeccable` and its `.github` mirror (skill + agent files).
  `705bf65`

- **2026-08-16** Patch js-yaml and nanoid to close 2 of 3 open Dependabot alerts (Rajesh) — asked
  to check Dependabot for updates; pinned js-yaml `^4.3.1` (fixes quadratic CPU consumption,
  CVE-2026-59870) and nanoid `^3.3.18` via `package.json` overrides, both transitive through
  astro/postcss. The remaining alert (`image-size`, via `@netlify/blobs`) has no patched release
  upstream yet. `84e2df1`

- **2026-08-15** Fix bad image crops in the homepage Explore/Engage grids (Rajesh) — flagged the
  Explore & Engage section images as cropped in the wrong places; the People and "bring your
  team" cards were center-cropping a selfie headshot and a 4-panel collage into a wide strip,
  and Resource Centre/Events were cropping full-size photos meant for other pages. Swapped in
  better-suited photos and purpose-cropped thumbnails, and switched both grids from a fixed
  pixel image height to CSS `aspect-ratio` so the crop stays consistent across the 3-col/2-col/
  1-col breakpoints instead of ballooning past 6:1 at in-between widths. `857c47b`

- **2026-08-13** Fix white knockout orange to match the source artwork exactly (Sharath) —
  the prior fix substituted the dark-ground orange token (`#F79A3C`) for the three seed paths
  instead of keeping the original (`#F88722`), a visibly different shade, flagged directly. Now
  matches the colour mark's orange exactly. `de20a18`

- **2026-08-13** Fix white knockout logo losing the three orange seed accents (Sharath) —
  flagged directly ("this now misses the 3 orange leaves"); the knockout script had recoloured
  every path white indiscriminately instead of just the green roundel path. Now only the roundel
  goes white; the three orange-seed paths become `#F79A3C` (the design system's existing
  dark-ground orange token). Regenerated the mark and both lockups built from it, and removed
  the now-superseded colour-on-green footer variant. `a965870`

- **2026-08-13** Switch Footer to the white knockout logo lockup (Sharath) — flagged the colour
  lockup's frond nearly disappearing into the footer's green background; traced to the "white"
  frond actually being negative-space cutouts that show whatever's behind the mark, not real
  white fill — fine on the cream page background, not fine on a green one. Added
  `tvc-logo-horizontal-footer-white.svg` (solid-white-fill paths, no colour dependency) at
  Footer's own geometry. `39a668e`
- **2026-08-13** Stub `/pagefind/pagefind.js` in dev to stop a fatal HMR overlay (Sharath) — the
  overlay (a pre-existing dev-mode-only quirk, unrelated to app code) was intermittently covering
  the whole page including the footer, surfacing as "the logo isn't visible" during this same
  review; added a dev-only Vite middleware serving a minimal stub, `apply: 'serve'` so production
  builds are untouched. `9716359`

- **2026-08-13** Migrate Footer.astro to the vector logo lockup too (Sharath) — Footer sits on
  `--tvc-green-dark` but keeps the mark in brand colour with only the text turned white, a
  combination neither existing lockup matched; built a third variant,
  `tvc-logo-horizontal-footer.svg`, sized to Footer's own metrics. Quicksand is now unused
  site-wide as a result (left in `global.css`, not removed — separate decision). `f4391d4`
- **2026-08-13** Wire the horizontal logo lockup into Nav.astro (Sharath) — asked to put the new
  logo asset to use; replaced `.nav__brand`'s PNG-mark + live-text composition with
  `tvc-logo-horizontal.svg` (swapping to the mark-only SVG below the existing 530px breakpoint,
  same responsive behaviour as before). The wordmark trades live/selectable text for vector
  shapes; `Footer.astro`'s separate brand lockup wasn't touched. `d067bcc`
- **2026-08-13** Add vector logo variants: white knockout mark, horizontal lockup (Sharath) —
  pointed at the collective's vector artwork on Google Drive to finish the last design-system
  "To build" item; added `tvc-logo-mark.svg` (that artwork, now scalable — previously only
  PNG/JPG rasters existed), a white knockout variant, and a mark+wordmark horizontal lockup in
  colour and white. The wordmark is real vector outlines of self-hosted Quicksand (matching
  `Nav.astro`'s treatment), not live text, extracted via `fontTools`. Reserve assets, not used
  anywhere yet. `ddf7813`
- **2026-08-13** Add a reusable `.callout` component using Earth Brown/Sky Blue (Sharath) — asked
  to implement one of the remaining design-system gaps; generalized `.rainfall-summary`'s
  existing bordered-card pattern (`WeatherView.astro`) into `.callout`/`.callout--sky` in
  `global.css`, and added `--tvc-sky-blue-text` since `--tvc-sky-blue` itself fails WCAG AA as
  text. Reserve only, no page consumes it yet. `0a31304`
- **2026-08-13** Add a spacing scale and motion tokens (Sharath) — asked to implement two of the
  design-system gaps flagged earlier; added `--space-1`–`--space-8` (sized from an audit of the
  site's ~40 existing hand-placed spacing values) and `--transition-fast`/`--transition-base`
  (sized from the dominant existing transition timings) to `global.css`. Reserve tokens only, no
  existing component retrofitted. `7390f5b`
- **2026-08-13** Fix `.eyebrow`'s failing WCAG AA text contrast (Sharath) — asked for ideas on
  improving the design system, which turned into a contrast audit of the newly-unified palette;
  `--tvc-orange` (`#bd7248`) only clears 3.46:1 against the cream page background, below the
  4.5:1 small-text needs, and every section eyebrow site-wide used it. Added
  `--tvc-orange-text` (`#965836`, same hue/saturation darkened to 5.23:1) for `.eyebrow` to use
  instead; `--tvc-orange` itself untouched elsewhere. `9b5e411`
- **2026-08-12** Process Divya & Sudhir's member profile update (Sharath) — folded in their
  "TVC Members: Your Story" submission (bio/family/whyTVC/social, new photo); fixed two spelling
  slips in their own free-text answers per confirmation. `2f38331`
- **2026-08-12** Process 4 member profile updates, add a member-photo lightbox (Sharath) —
  Sree & Prameela, Sundar & Anitha, Om & Pavithra, and Ram & Krishna's "TVC Members: Your Story"
  submissions folded into their cards (bio/family/whyTVC/social, two new photos); also enlarged
  the popup photo and made it click-to-enlarge, backed by a new uncropped `-full` photo variant
  (`fetch-member-photo.mjs`) since the existing square crop alone lost context from group photos.
  `95b3f37`
- **2026-08-13** Add shared reserve colour tokens for a unified TVC design system (Sharath) —
  comparing tvc.farm's design system against docs.tvc.farm's (a separate Quartz/Obsidian site)
  turned into drafting a reconciled spec, documented in the TVCDocs vault's "Design system" note;
  the two palettes already matched almost everywhere, so this repo only gained three new reserve
  tokens (`--tvc-green-vivid`, `--tvc-earth-brown`, `--tvc-sky-blue`) in `global.css`, not yet
  used anywhere. The bigger change landed on the docs.tvc.farm side (Quartz config + fonts
  switched to self-hosted), outside this repo. `eab8b0d`
- **2026-08-11** Fix the two real issues left after the `impeccable detect` audit (Sharath) —
  after ruling the other four flagged categories false positives (photo-hero contrast, JS-driven
  image placeholders, callout/accent `border-left` styling, and a small-UI-text size cluster all
  traced back to intentional patterns the static analyzer can't see the context for), two held up:
  the journey timeline's scroll-progress rail animated `height` on every scroll event instead of
  `transform: scaleY()`, forcing layout every frame; and the staff page's uppercase role label
  added letter-spacing tracking that's dead weight on Kannada/Tamil (no letter case) while making
  an already-longer translated label wider — scoped off via `:lang(kn)`/`:lang(ta)`. `d1033d4`
- **2026-08-11** Fix skipped heading level in event page content (Sharath) — asked for an
  `impeccable detect` audit of current design/accessibility issues, which flagged all 9 events
  (en/kn/ta, 27 files) jumping straight from `##` to `####` with no `###` in between; confirmed no
  file used `###` anywhere, so it was a clean level rename with no collisions. `3eb58f3`
- **2026-08-11** Rename three member cards (Sharath) — Anil & Roopa, Lakshmi & Vijay, and
  Nishant & Neeti were listed under a partner's name that no longer applies; updated to
  Roopa PN, Lakshmi Ratnaparke, and Nishant Gupta respectively. `8465b8b`
- **2026-08-11** Sort member cards alphabetically on the Members page (Sharath) — noticed the
  tiles at /people/members weren't in order; the `members` array in `src/data/members.ts` had
  drifted out of alpha order as entries were appended over time. Now sorted by first name in
  `MembersView.astro` so future additions land correctly without hand-placement. `43b890b`
- **2026-08-10** Show a graceful message when the chatbot is out of API credits (Sharath) —
  discussed hiding the chat icon proactively during an outage but chose the simpler fix: `chat.mts`
  now detects Anthropic's 403 `billing_error` specifically and returns a warmer message instead of
  the generic "having trouble" catch-all; no client-side changes needed. `568a128`
- **2026-08-10** Fix "Sorry, I couldn't come up with an answer" on chat follow-up questions
  (Sharath) — root-caused via a raw API call reproducing it: Claude Sonnet 5 sometimes returns a
  leading `thinking` block before its `text` block (more often on multi-turn requests), and
  `chat.mts` only ever checked `content[0]`, discarding a perfectly good answer sitting at
  `content[1]`. Now finds the first `text` block anywhere in the array. `358d6dc`
- **2026-08-10** Persist chat widget history across page navigations (Sharath) — follow-up to the
  retrieval rework below; the widget's conversation lived in a plain in-memory array and silently
  reset on every page load, which stood out on a multi-page site where visitors often browse a
  couple of pages while following up on a question. Now persisted to `sessionStorage` (clears on
  tab close) and rehydrated on open. `916a68d`
- **2026-08-10** Replace the chatbot's full-corpus prompt with per-query retrieval (Sharath) —
  chased down "Sorry, I couldn't come up with an answer" and an intermittent "couldn't reach the
  server" to `chat.mts` stuffing the entire ~28.5k-token site corpus into every request, risking
  the function's ~10s Netlify timeout on a prompt-cache miss. Added a keyword-overlap retrieval
  step (a fixed core page set plus a handful of query-matched pages) instead, cutting typical
  requests to ~3-10k tokens; also matches the message against `src/data/members.ts` directly and
  injects the full record when relevant, closing a real gap where family/why-TVC content (popup-
  only, client-rendered from JSON) was invisible to the chatbot no matter how retrieval was tuned.
  `a3d38a2`
- **2026-08-10** Make site search jump to the matched member's card (Sharath) — searching a
  member's name found them but never scrolled to or opened their card; Pagefind's chunking merges
  many of the ~130 near-identical member cards together, so its deep link for a hit could point at
  the wrong member entirely with no in-page anchor. Added a separate client-side name lookup
  (`src/data/members.ts`, `SiteSearch.astro`'s `searchMembers()`) that's always attributed
  correctly, plus a stable per-card anchor id and hash-driven auto-open/scroll on the Members page.
  `ef32610`
- **2026-08-10** Fix Sree & Prameela's photo orientation, add a reusable photo-fetch script
  (Sharath) — the photo backfilled in #69 rendered rotated 90 degrees; the ad-hoc download+resize
  script used there didn't apply EXIF auto-orientation before cropping, baking in the wrong
  rotation. Added `scripts/fetch-member-photo.mjs` as the one script this logic lives in going
  forward instead of a hand-rolled version per batch. `a6af555`
- **2026-08-10** Switch site search from a title/description index to Pagefind (Sharath) — member
  names weren't findable via search since the old index only covered each page's
  `<title>`/`<meta description>`, not body content; replaced `scripts/build-search-index.mjs`
  with the Pagefind CLI, which indexes full page content (scoped to `<main>` via
  `data-pagefind-body`, still skipping `noindex` pages) as a static, chunked index that
  `SiteSearch.astro` queries client-side — same no-backend approach, real full-text results.
  `a41d978`
- **2026-08-10** Pull in the three member photos blocked on Drive access (Sharath) — the
  `tvc-photo-pool` service account is now shared on the "TVC Members: Your Story" upload folder;
  downloaded and set the photos for Udhay & Lavanya, Sameet & Soumya, and Sree & Prameela that
  the last two batches couldn't fetch. `46daef2`
- **2026-08-10** Support per-person why-TVC quotes and process Sree & Prameela's submissions
  (Sharath) — the two rows in the response Sheet were separate answers from each partner, and
  the card schema only had room for one shared quote; widened `whyTVC` to accept a list so each
  person's own words render as their own attributed quote bubble instead of picking one or
  paraphrasing both together. Prameela's attached photo hit the same Drive access gap as the
  prior batch. `1755857`
- **2026-08-09** Process three new "TVC Members: Your Story" form submissions (Sharath) —
  routine batch from the response Sheet; updated Udhay & Lavanya, Sameet & Soumya, and Ramesh &
  Sarika's cards on the Members page with third-person bio/family text, why-TVC quotes, and
  LinkedIn links. Two rows had a new photo attached that couldn't be pulled in (service account
  lacks Drive access to those files) — follow-up needed to fix that access. `1507b0f`
- **2026-08-09** Correct stale "not verified against production" claims in ARCHITECTURE.md
  (Sharath) — asked to spot-check the doc's production-status table after the Photo Pool
  dashboard's "not yet pushed to main" line didn't match what was actually live at
  `/internal/photo-pool`; direct checks (page/API status codes, and Netlify's own Forms API for
  the two inquiry forms) confirmed Photo Pool, site search, and the Visit inquiry form (which has
  a real submission) are all live and verified, and surfaced a genuine gap: the Host an Event form
  is correctly registered with Netlify but has zero submissions to date. `eaeab5b`
- **2026-08-09** Add page-specific JSON-LD schema to the remaining content pages (contact) —
  following up on an AI-search-optimization review that found only Visit/Events/a couple of
  Article pages had page-specific schema beyond the sitewide Organization/WebSite/BreadcrumbList
  graph; added AboutPage, VideoObject, CollectionPage, Product+Offer (parsed off each page's own
  pricing table), Service, Person per staff member, Organization per partner, and ContactPage
  across About, Ecosystem, Visit's Camping/Day Visit/Host an Event/Trekking Trails, People's
  Staff/Partners/Outreach, Resource Centre, In Pictures, Our Journey, Contact, and the Events
  index — deliberately skipping Members, Weather/Biodiversity, Join, and Home where a type would
  mean fabricating facts the page doesn't state. `222684f`
- **2026-08-08** Highlight children/pet names, drop the now-redundant bottom link row, refresh
  the story-guide screenshot (Sharath) — with names already linking inline in the bio/family
  text, the popup's bottom row of platform icons duplicated the same links; removed it (and the
  now-dead `socialIcon()`/icon-row code) since the inline treatment is the only affordance now.
  Added a `familyNames` field so children/pets mentioned in `family` (e.g. Sharath & Deepa's
  kids and cat) get the same bold highlight as the card's own people, never linked since no
  social profile is ever collected for them. Replaced the story-guide page's stale
  `card-example.jpg` (still showing the old two-photo/LinkedIn-Instagram-icons mockup) with a
  real screenshot of the live, redesigned popup, and corrected its alt text to match.
- **2026-08-08** Read the member-story response Sheet via API instead of the Sheets UI (Sharath)
  — clicking through cells to check for new/updated form responses didn't scale; added
  `updateSheetValues()` to `google-drive.mjs` (only `appendSheetRow()` existed before, which
  can't write in place) and `scripts/check-member-story-responses.mjs`, which lists rows whose
  Timestamp is newer than their own "Processed at" column (catches edits too, not just first
  submissions — editing a response overwrites its row rather than adding a new one) and can mark
  a row processed. Shared the Sheet with the existing `tvc-photo-pool` service account as an
  Editor; along the way fixed the Sheet's own header row, where an earlier UI edit had
  accidentally typed "Processed at" and "Notes" into a single cell with a literal tab character
  between them instead of two separate cells.
- **2026-08-08** Redesign the member card summary and popup for crispness/clarity (Sharath) —
  reviewing the Sharath & Deepa card live surfaced that a blindly-truncated first-person form
  bio reads poorly on the card, and the popup's centered, same-weight text blocks felt cluttered;
  added a hand-written `summary` field (covering both people on a couple's card), left-aligned
  and hierarchy-differentiated the bio/family text, turned the why-TVC quote into a speech-bubble
  callout, normalized narration to third person (quote stays first-person on purpose), bolded/
  linked each person's first name inline, flex-pinned "More" to the card's bottom so a short
  summary doesn't strand it above its row, and fixed a `socialLinks()` bug that dropped link
  attribution whenever a multi-person card had only one submitted URL.
- **2026-08-08** Fill in Sharath & Deepa's member card with their real "Your Story" form
  response (Sharath) — testing the end-to-end member-profile-update pipeline (form → review →
  merge to feature branch → merge to production) surfaced that the member-story feature (PR
  #60) had already merged to main with placeholder test data still on that card (a duplicated
  photo, `example.com` social links); replaced with the real submitted bio, family, why-TVC
  quote, and LinkedIn. `6d7ac85`, `25b52c6`
- **2026-08-08** Add a sitemap/search-performance check script (Sharath) — now that
  tvc-photo-pool is a Full Search Console user, added `listSitemaps()`/`querySearchAnalytics()`
  to `google-drive.mjs` and `scripts/check-search-performance.mjs`, which reports sitemap
  crawl/warning/error status plus real site-wide clicks/impressions and top pages/queries for a
  given window (default 28 days) — the two report types `check-search-console.mjs` can't reach
  since it only inspects one URL at a time.
- **2026-08-08** Document the tvc-photo-pool service account's Search Console upgrade to Full
  user (Sharath) — Sharath upgraded the account from Restricted to Full directly in the Search
  Console UI, which (verified against the live API) unlocks `sitemaps.list` and
  `searchAnalytics.query` in addition to the existing `urlInspection.index.inspect`; corrected
  the now-stale "Restricted user" references in `ARCHITECTURE.md` and `google-drive.mjs`. No
  script uses the newly-unlocked endpoints yet.
- **2026-08-08** Fix the same double-hop redirect chain in PR #55's newly-added URLs (Sharath) —
  PR #55 (Search Console's 2026-08-07 Coverage export) landed 14 more `netlify.toml` redirects
  right after the double-hop fix below, at extension-less targets again; repointed all 14 at
  their trailing-slash canonical URL the same way.
- **2026-08-08** Collapse double-hop redirect chains in netlify.toml (Sharath) — running the new
  `check-search-console.mjs` against every previously-flagged URL surfaced `Redirect error` on
  many of them; traced it to custom redirects pointing at extension-less paths (e.g. `/contact`),
  which Netlify then 301s again to the trailing-slash form it actually serves, so Googlebot saw
  two hops instead of one. Repointed all directory-style redirect targets straight at their
  trailing-slash canonical URL (root `/`, the one file redirect, and `:splat` rules left alone).
- **2026-08-07** Redirect more of Search Console's Pages-report URLs (contact) — a fresh Coverage
  export flagged 32 URLs as `Pending`/`Failed`; 18 already had 301s from earlier passes and just
  hadn't been recrawled yet, added 301s in `netlify.toml` for the other 14 — old `.html`/bare
  product and event-post slugs (tamarind chutney, Lantana furniture, three event dates matched
  against `src/content/events`, a partner with no dedicated page), the Tamil-locale sibling of the
  existing `/people/community-outreach` rules, and `/ecosystem/geography` (the pre-Astro name for
  `/ecosystem/landscape`, confirmed via its `heroTitle`).
- **2026-08-07** Add `llms.txt` (Rajesh) — following an SEO/AEO/GEO audit of the site, added a
  curated, spec-compliant index of key pages (Visit, Events, Ecosystem, About, People, etc.) with
  short descriptions at `public/llms.txt`, the emerging convention AI assistants check for a
  clean summary of a site's content.
- **2026-08-07** Add a Search Console indexing-check script (Sharath) — after landing a redirect
  fix, wanted a way to confirm Google actually recrawled the affected URLs instead of manually
  re-checking Search Console; added the `webmasters.readonly` scope to the existing Drive/Sheets
  service account (added as a Restricted user on the `tvc.farm` property) and a new
  `inspectUrl()` helper in `google-drive.mjs`, plus `scripts/check-search-console.mjs` to print
  `coverageState`/`lastCrawlTime` for one or more URLs. Local-only, no new Netlify env vars.
- **2026-08-07** Fix skipped heading levels in event markdown content (AshSgDe29071999, merged by
  Sharath) — two external PRs (#53, #54) both closed issue #52 by promoting each event's first
  `###`/`####` body heading to `##`; merged #54 since it also covered the `kn`/`ta` translated
  copies that #53 missed, and closed #53 as superseded. `08eda1f`
- **2026-08-06** Fix 6 skipped-heading-level components found in a full-site Impeccable audit
  (Sharath) — a broader `impeccable detect` pass against the built `dist/` (166 pages, all 3
  locales) surfaced 72 `h1`→`h3`/`h4` heading skips; 6 were component-level (Members/Staff card
  names, the event "Want this to happen again?" widget, About Design's "Aerial view", Day Visit's
  itinerary cards, How to Reach's two sections, Trekking Trails' trail cards) and got promoted to
  `h2`, with explicit `font-size` overrides added wherever the tag change would otherwise have
  visibly enlarged a card label. Verified pixel-identical rendering across all 6 pages and a clean
  `npm run build`. The remaining 33 skips live in event markdown content itself (`### `/`#### `
  as the first heading) — left as a separate, larger content-authoring cleanup. `64e975a`
- **2026-08-06** Patch fast-uri, undici, and postcss for known CVEs (Sharath) — GitHub flagged 6
  Dependabot alerts (2 high, 4 moderate) after a push; `npm audit fix` bumped all three transitive
  deps to patched versions with no `package.json` range changes needed (fast-uri via
  `@astrojs/check`'s dev-only type checker, undici via `cheerio` — used only in the local
  build-time `scripts/build-chat-context.mjs`, not any served/runtime path — and postcss via
  astro/vite's build tooling). None sat in the site's shipped or request-time code, but the fix
  was free; verified with a full `npm run build`. All 7 alerts (incl. an already-fixed Astro XSS
  advisory) now show `fixed` on Dependabot. `c9aed45`
- **2026-08-06** Fix design/accessibility nits found in a design review (Sharath) — asked for a
  design review of the live site, done manually plus a pass with the `impeccable` design-lint CLI
  against the running dev server; fixed the In Pictures hero eyebrow mislabeled "About Us" instead
  of "Explore", added real line icons to the homepage's 4 value-strip circles (previously empty
  placeholder dots), fixed a skipped heading level (`h2`→`h4`) under /visit's "Etiquette &
  precautions", bumped two under-12px UI text spots (chat widget subtitle, photo-gallery filter
  labels) above the legibility floor, and trimmed two homepage eyebrow labels that were full
  sentences set in small tracked-out caps. Left the homepage's actual tagline alone since it's
  reused verbatim in the footer/meta description. `ef33514`
- **2026-08-06** Install the Impeccable design skill for Claude Code and GitHub Copilot
  (Sharath) — wanted AI-driven UI work to follow a consistent design language instead of
  generic defaults; ran `npx impeccable install --providers=claude,github`, then `/impeccable
  init` to capture product context in `PRODUCT.md` (primary user: prospective visitors
  deciding whether to camp/day-visit/trek/attend an event). Project-scoped only (`.claude/`,
  `.github/`), nothing changed globally. `403702b`
- **2026-08-05** Make the rainfall chart more interactive, explain the two-monsoon pattern
  (Sharath) — split the monsoon-season shading into two bands (pre-monsoon Apr-May, retreating
  monsoon Aug-Oct) sized to each month's actual band width rather than point-to-point, matching
  the rain-shadow dip visible in TVC's own data; added legend hover-spotlight, a monthly/
  cumulative toggle, draw-in animation, and per-year hover point markers; rewrote the intro copy
  and turned the ENSO context sentence into a bordered callout card. `cf987fc`
- **2026-08-05** Fix the rainfall chart/table not showing on production, redesign as a multi-year
  line chart (Sharath) — `RAINFALL_SHEET_ID` had never actually been set on Netlify despite docs
  claiming it was, so `/api/rainfall` 500'd and the page silently fell back to "data isn't
  available"; set the env var across all deploy contexts and adapted `rainfall.mts`'s Sheet
  parsing to the "Rain data monthly"/"Daily rain data" tabs' new calendar-year-native structure
  after Sharath restructured them. Also replaced the single-year bar chart with a smooth Jan-Dec
  line per calendar year (colorblind-safe palette, hover/keyboard tooltip, legend checkboxes to
  show/hide individual years), dropped the repeated "mm" unit from every table cell, and added a
  short context sentence under the monsoon stat that recomputes live and cites a real source when
  available. `a2d7efc`
- **2026-08-04** Sync the rainfall chart/table/monsoon stat live from Google Sheets (contact) —
  the `/ecosystem/weather` rainfall numbers were a hand-copied snapshot baked into
  `WeatherView.astro`, updated by hand whenever someone remembered to; added
  `netlify/functions/rainfall.mts` to read the community's "Tvc rain data" Sheet (monthly + daily
  tabs) live on every page view, and moved the chart/table/"this monsoon so far" stat to
  client-side rendering (`src/utils/rainfall.ts`) so a new row logged in the Sheet shows up on the
  site with no code change or rebuild. `RAINFALL_SHEET_ID` set on Netlify. `5e57daf`
- **2026-08-04** Fix all four homepage stats sitting frozen at 0 for several seconds (contact) —
  a screen recording from the live site showed Acres/Years/Families/Species all stuck at "0" for
  several real seconds before jumping to their final values together; traced to a static
  top-level import in `HomeStats.astro`'s client script forcing the browser to fetch/parse an
  unrelated observation-utils.js → tvc-boundary.js chain before the shared entrance-animation
  observer could even be set up. Converted it to a dynamic `import()`, which surfaced a second
  issue (Vite bundling that dynamic import together with Leaflet via observation-utils.ts's other
  static importer) fixed by splitting the species-count logic into its own dependency-free
  `species-count.ts`. Verified 4370ms→1500ms under identical simulated slow-network conditions.
  `ce49f62`
- **2026-08-04** Bake the homepage species count into the static HTML at build time (contact) —
  the earlier localStorage-cache fix for the "—" species tile only helped repeat visitors in the
  same browser; contact reported it still didn't show immediately on real (non-repeat) traffic.
  `HomeStats.astro` now fetches the count itself at build time and bakes it in via the same
  `data-count-to` mechanism the Acres/Years/Families tiles already use, so every visitor gets the
  same instant animated-count-up treatment, not just returning ones; the deferred client-side
  fetch still runs to quietly correct the number if new sightings came in since the last deploy,
  and falls back to the previous cache-based behavior if the build-time fetch fails. `db79dec`
- **2026-08-04** Cut ~1MB of oversized homepage images from a PageSpeed audit (contact) — a
  PageSpeed Insights lab report flagged "Improve image delivery" (est. 1.6MB) as the homepage's
  biggest remaining opportunity; the 362x140 explore-grid cards were pulling in full-size hero/
  display photos meant for other pages. Swapped the In Pictures card to the R2 "thumb" variant
  curate-photos.mjs already uploads (698KB→~90% smaller), generated dedicated thumbnails for the
  About/Biodiversity cards, pointed Our Journey at the door section's existing thumb instead of
  the full hero photo, regenerated that thumb plus the while-at-farm door thumb and the video
  poster at tighter sizes/quality, and shrank the logo mark 200x200→120x120 in place (used
  everywhere it appears, no reference changes). `56d428c`
- **2026-08-04** Show a cached species count instead of a blank homepage stat tile (contact) —
  while investigating why the mobile PageSpeed score looked worse after the hero-video fix
  above, flagged that the stats bar's species tile shows "—" until the deferred iNaturalist
  fetch resolves after `window.load`; added a small dedicated localStorage cache (just the
  count, not the ~5MB raw-observations payload) read synchronously on script init, so a repeat
  visit shows the real number immediately instead of the placeholder. `9cab4f6`
- **2026-08-04** Stop the homepage hero video downloading on mobile (contact) — mobile PageSpeed
  Insights performance was low; the already-re-encoded 530KB `home-hero.mp4` (see the 2026-08-01
  PageSpeed pass below) still had `preload="auto"` and an eager `src`, so every visitor —
  including mobile — fetched it unconditionally, competing with fonts/CSS for bandwidth. The
  `<video>` now carries no `src` in markup and only gets one (plus autoplay) once JS confirms a
  desktop-width (≥860px), non-reduced-motion viewport; mobile visitors see the existing poster
  image with zero video bytes downloaded. `237bba1`
- **2026-08-03** Fix Staff card photo crops (Sharath) — the object-fit:cover crop was cutting
  Krithika out of Madhavan's card entirely (a wide selfie centered into a narrow portrait box),
  and separately Sharath flagged inconsistent zoom/framing across the couple cards; after two
  rounds of auto-cropping still felt off, Sharath supplied his own square (1:1) crops for all 6
  cards, which the card layout now matches. `1a83be8` `221f6e3`
- **2026-08-03** Add Krithika to Madhavan's Staff card (Sharath) — a follow-up to the Staff
  update above; swapped Madhavan's solo photo for a family selfie with wife Krithika and
  daughter Shashtika, and added Krithika as a second person on the card (teacher, born and
  raised in Trichy, teaches classes 1-3 at the government school in nearby Arthakal). `9f0f7d6`
- **2026-08-03** Add photos and expanded bios to the Staff page (Sharath) — asked to update Staff
  with fuller write-ups and photos for each person; replaced the plain grid with photo cards,
  corrected two names (Ramdoss→Ramdhas, Yellaraju→Elraju), expanded every bio, and added the
  three staff couples (Gundappa & Mahadevamma, Puttappa & Mahadevamma, Chudesa & Puttamadevi) as
  paired cards sharing one photo each. `4c2bcc6`
- **2026-08-03** Redirect Search Console's Server error (5xx) URLs (contact) — follow-up on the
  404 cleanup; added 301s in `netlify.toml` for the 6 flagged URLs (bare siblings of already-fixed
  `.html` pages, plus `/day-visit.html` and `/soil-and-weather-conditions`) — this static site has
  no code path that can actually throw a 500, so likely stale crawl records, fixed regardless.
  `1b11fdc`
- **2026-08-03** Redirect the rest of Search Console's 404'd URLs (contact) — Search Console's
  Coverage export flagged 20 URLs as `Not found (404)`; two already had 301s and just hadn't been
  recrawled, added 301s in `netlify.toml` for the other 18 (old `.html` pages, a stray
  extensionless duplicate, a direct PDF link, and a few slug variants) to their current
  equivalents. `4593381`
- **2026-08-02** Crop the Staff hero photo tighter on the team (Sharath) — the top of the source
  photo was mostly tree canopy, pushing the staff themselves down into the hero's fade zone;
  cropped off the top ~280px (1280×963→1280×683) so they read clearly in the visible banner area.
  `8ddc203`
- **2026-08-02** Update the Staff page banner photo (Sharath) — swapped the plain (photo-less)
  Staff hero for a group photo of the on-site team; added `/images/pages/staff/hero.jpg` and
  wired it into `PageHero`/`BaseLayout` the same way `/people/members` uses its hero photo.
  `4f4d56a`
- **2026-08-01** Fix the cookie banner's slow first paint (Sharath) — asked to look into why the
  cookie-consent text was showing up as the LCP element on lighter pages like `/people/members`;
  its script was a deferred JS module even though its HTML was already in the DOM, so it took
  2s+ to un-hide for first-time visitors. Made it `is:inline` (render delay 2125ms→130ms); also
  skipped a redundant hero-image opacity write on page load in `PageHero.astro`/`HomeView.astro`,
  though that one didn't measurably move the LCP number. `6591c72`
- **2026-08-01** Stop hotlinking a journey photo from GitHub (Sharath) — asked to check why
  other pages' PageSpeed LCP was worse than the homepage's; `/our-journey`'s July-Madhavan photo
  was pulled from a github.com/user-attachments link (extra redirect hop, 795KB PNG, and a real
  reliability risk since that hosting isn't meant for production hotlinking). Downloaded it,
  converted to JPEG, hosted locally like every other journey photo: 795KB→78KB. `a942165`
- **2026-08-01** Re-encode the homepage hero video (Sharath) — the last piece of the PageSpeed
  pass; installed ffmpeg and re-encoded `home-hero.mp4` (libx264, CRF 32), the single largest
  resource on the homepage, from 2083KB to 530KB with no visible quality loss. `6526bfd`
- **2026-08-01** Cut image/video payload from a Lighthouse/PageSpeed audit (Sharath) — continued
  the PageSpeed pass; right-sized the homepage video poster (374KB→188KB), the homepage door-card
  thumbnails (WebP + lazy-loaded, were loading eagerly and oversized), and the logo mark
  (695px source for a 56px max display size, 104KB→30KB); deferred the ~470KB iNaturalist
  species-count fetch until after `window.load`; fixed `<meta charset>` ordering to stay within
  spec's 1024-byte window regardless of server headers. `6fc4770`
- **2026-08-01** Fix accessibility issues from a Lighthouse/PageSpeed audit (Sharath) — asked
  what PageSpeed Insights would flag; fixed the CTA button's failing white-on-orange contrast
  (now dark ink on the same orange), a skipped h2→h4 heading level on the homepage Explore
  cards, and preloaded the Fraunces display font. Accessibility score 95→100 locally. `11303f7`
- **2026-08-01** Add Shilpa & Aditya's photo to the Members page (Sharath) — their profile had no
  photo; Sharath supplied their family portrait, cropped and resized to the site's 400x400
  member-avatar convention. `88b4651`
- **2026-08-01** Update Shilpa & Aditya's bio on the Members page (Sharath) — Shilpa updated her
  profile in the source Members Google Slides deck; pulled the new bio (development finance
  background, published children's book) onto the site. `b46ec47`
- **2026-08-01** Use the 2026 city meet-up photo for the Members page hero (Sharath) — wanted
  the members page banner to show the 2026 members meetup instead of a single couple's portrait;
  swapped the hero/share image to `journey-43.jpg` and fixed that photo's Journey timeline
  caption, which had mislabeled it a "2019" meet-up instead of 2026. `c3cfc7c`
- **2026-08-01** Switch the Ananas project page's share thumbnail to the GAIA map (Sharath) —
  the link-preview image (og:image/twitter:image and the Article schema image) was the 2020
  syntropic design instead; swapped to `gaia-map.jpg` with its correct 1800×2563 dimensions.
  `414afe4`
- **2026-08-01** Add a dedicated Ananas project page (Sharath) — wanted a summary of Ananas's
  TVC work published on the site; the actual Google Drive archive turned out to hold a real
  multi-year design engagement (2017–2020+), so this expanded the Partners card's one-line fact
  with accurate specifics and added a full `/people/partners/ananas` page with a chronological
  timeline and images sourced from that archive and the Design Document itself (zone plans, the
  GAIA map, a bed-section diagram, the 2020 syntropic design). Also fixed a global CSS bug
  (`img` missing `height: auto`) that surfaced while adding these images. `7ce79ef`
- **2026-08-01** Require dev-server verification before closing (contact) — make sure changes
  are actually rendered and checked with the user, not just built; added a requirement to the
  "Development" section of `AGENTS.md`/`CLAUDE.md` to always render in the dev server and
  verify before closing it. `7e82ca9`
