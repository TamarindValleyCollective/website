---
import CommunityOutreachDetailView from '../../../../components/views/CommunityOutreachDetailView.astro';
import { getCollection } from 'astro:content';

export async function getStaticPaths() {
  const posts = await getCollection('communityOutreach', (entry) => entry.id.startsWith('kn/'));
  return posts.map((post) => ({ params: { slug: post.id.replace(/^kn\//, '') }, props: { post } }));
}

const { post } = Astro.props;
---

<CommunityOutreachDetailView lang="kn" post={post} />
