---
import BaseLayout from '../../layouts/BaseLayout.astro';
import PageHero from '../../components/PageHero.astro';
import BookingInquiry from '../../components/BookingInquiry.astro';

const itineraries = [
  {
    name: 'Picnic at the Farm',
    desc: 'A quiet, relaxed day.',
    schedule: [
      ['09:00', 'Arrive at TVC'],
      ['09:30', 'Breakfast'],
      ['10:30', 'Farm Tour / Foraging'],
      ['12:00', 'Juice / Drink & Relax'],
      ['12:30', 'Farming 101'],
      ['13:30', 'Lunch'],
      ['14:30', 'Board Games / Discussions'],
      ['16:30', 'Tea / Coffee'],
      ['17:00', 'Depart'],
    ],
  },
  {
    name: 'Hiking Adventure',
    desc: 'A more active day.',
    schedule: [
      ['09:00', 'Arrive at TVC'],
      ['09:30', 'Breakfast'],
      ['10:30', 'Hiking'],
      ['13:00', 'Re-hydrate and recover'],
      ['13:30', 'Lunch'],
      ['14:30', 'Land Restoration & Agriculture / Discussions'],
      ['16:30', 'Tea / Coffee'],
      ['17:00', 'Depart'],
    ],
  },
  {
    name: 'Do Nothing',
    desc: 'Exactly what it sounds like.',
    schedule: [
      ['09:00', 'Arrive at TVC'],
      ['09:30', 'Breakfast'],
      ['10:30', 'Board Games / Discussions / Take a nap'],
      ['13:30', 'Lunch'],
      ['14:30', 'Nap / Discussions / Board Games'],
      ['16:30', 'Tea / Coffee'],
      ['17:00', 'Depart'],
    ],
  },
];
---

<BaseLayout title="Day Visit" description="Three ways to spend a day at Tamarind Valley Collective: a relaxed picnic, a hiking adventure, or doing absolutely nothing." image="/images/pages/home/gallery-2.jpg" imageWidth={768} imageHeight={329}>
  <PageHero eyebrow="Visit TVC" title="Day Visit" intro="Choose between a quiet, relaxed day or a more active one." image="/images/pages/home/gallery-2.jpg" imageAlt="Visitors taking in the valley view from a hilltop" />

  <section class="section wrapper">
    <div class="itinerary-grid">
      {itineraries.map((it) => (
        <div class="card">
          <h3>{it.name}</h3>
          <p class="desc">{it.desc}</p>
          <table>
            <tbody>
              {it.schedule.map(([time, activity]) => (
                <tr>
                  <td class="time">{time}</td>
                  <td>{activity}</td>
                </tr>
              ))}
            </tbody>
          </table>
        </div>
      ))}
    </div>

    <p class="pricing-note">₹1,200 per person, ₹650 per child under 10 — includes breakfast, lunch, tea/coffee, and the activities in your chosen itinerary above. Confirm current pricing and availability when you send your inquiry below.</p>

    <BookingInquiry
      type="Day Visit"
      packageOptions={['Picnic at the Farm', 'Hiking Adventure', 'Do Nothing']}
      whatsappMessage="Hi! I'd like to know more about a day visit to TVC."
      note="Day visits are coordinated with our hospitality partner, Linger — send an inquiry or message us and we'll take it from there."
    />
  </section>
</BaseLayout>

<style>
  .itinerary-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 20px;
  }

  .desc {
    color: var(--tvc-ink-soft);
    margin-bottom: 14px;
  }

  table {
    width: 100%;
    border-collapse: collapse;
    font-size: 0.92rem;
  }

  td {
    padding: 6px 0;
    border-top: 1px dashed var(--tvc-line);
  }

  td.time {
    font-variant-numeric: tabular-nums;
    color: var(--tvc-green);
    font-weight: 600;
    width: 70px;
  }

  .pricing-note {
    margin-top: 24px;
    color: var(--tvc-ink-soft);
    font-size: 0.9rem;
  }
</style>
