# Change Log

> **Keep this file up to date.** Whenever a change discussed here is committed and pushed to
> `main`, add a row in the same change: date/time, who requested it, what was asked, and
> what was actually changed (with the commit hash(es)). Newest rows go at the top. See the
> note in `AGENTS.md`.
>
> Scope: this log tracks changes requested through and implemented by the AI assistant. It is
> not a full replacement for `git log` — teammates' own commits made outside that workflow
> aren't itemized here.

| Date | Summary | Requested by | Ask | Change | Commit(s) |
|---|---|---|---|---|---|
| 2026-07-26 12:37 | Add sitemap.xml, robots.txt, and noindex the two thank-you pages | Sharath Jeppu | "is there anything that needs to be done for SEO optimization?" then "yes, go ahead" | Audited the site's SEO setup first: per-page meta tags (title/description/canonical), Open Graph + Twitter cards with correctly-sized images, sitewide JSON-LD (Organization/WebSite plus Event/Article/FAQPage schema on relevant pages), a single H1 per page, and clean 301 redirects were all already solid. Found two real gaps and fixed both: no `sitemap.xml` (added the official `@astrojs/sitemap` integration, zero config beyond the `site` URL already set in `astro.config.mjs`) and no `robots.txt` (added one, pointing at the new sitemap). Also marked `/contact/thanks` and `/visit/host-an-event/thanks` `noindex` (same `BaseLayout` prop `/404` already uses) — thin, form-confirmation-only pages with no content worth ranking on. Filtered both thanks pages (and `/404`) out of the sitemap too, to avoid the mixed signal of listing a URL there while also telling crawlers not to index it. Verified with `astro build`: 40 real content URLs in the generated sitemap, cleanly excluding the thanks pages, `/404`, and every redirect-stub route; confirmed `robots.txt` serves correctly and both thanks pages carry the `noindex` meta tag in their built HTML. | `430df44` |
| 2026-07-26 12:29 | Fix the events filter bar sitting flush against the left edge | Sharath Jeppu | "the filter pills on teh event page now has an issue. Check this issue across the website", then clarifying "The issue is UI issue. It's too close to the left edge" | `.controls` on `/events` had `padding: 24px 0` (shorthand) on the same element as `.wrapper` (`class="controls wrapper"`) — a shorthand `padding` sets all four sides at once rather than merging with other rules, so it silently zeroed out `.wrapper`'s `padding: 0 24px`, pinning "Filter by type" and the chip row flush against the viewport edge. Confirmed the cause via `getBoundingClientRect()` (computed `padding-left` was `0px`) before touching anything. This is the exact bug class `global.css`'s own `.section` already carries a comment warning about for the same combined-with-`.wrapper` reason — switched `.controls` to the same longhand `padding-top`/`padding-bottom`-only pattern. Per the "check across the website" ask, audited every other page/component that combines a custom class with `.wrapper` on the same element for the same shorthand-padding conflict; `.controls` was the only instance. Verified visually and via computed styles before/after. | `6feb877` |
| 2026-07-26 12:20 | Add cover images for 6 of the 7 newly-imported events | Sharath Jeppu | "I have the images locally on my downloads folder. use them" | Found 6 clearly-named photos in `~/Downloads` matching 6 of the 7 events added earlier this session (verified each against its Facebook source before using), resized/re-encoded with `sharp` (max width 1600px, JPEG quality 82 — same convention `scripts/curate-photos.mjs` already uses) into `public/images/events/<slug>/hero.jpg`, and wired up via each event's `coverImage` frontmatter. The volunteer-day photo came in large (576KB, real photo detail rather than a poster) — re-encoded at a smaller width/quality to bring it in line with the site's typical hero image sizes (~448KB). No photo was supplied for the 19 Nov 2022 Star Gazing Camp specifically (only the Dec 2021 edition), so that one still has no cover image. Verified with `astro build` and in-browser — images bleed correctly to the Past-events grid card edges and render as the page hero with the site's fixed-fade effect. | `32e3d6c` |
| 2026-07-26 10:57 | Add 7 past events found on Facebook but missing from the site | Sharath Jeppu | "See if all the events on our facebook page are available in our events page. https://www.facebook.com/farm.tvc/past_hosted_events" then "sure" (confirming the offer to draft content for the gaps found) | Pulled the full "Past events" history from `facebook.com/farm.tvc/past_hosted_events` (78 events back to 2017) and cross-checked against the 12 events already on `/events`. Most 2020+ events matched; found 3 site events not on Facebook at all (Camp Night & Visioning, Introduction to Regenerative Thinking, The Lambda Retreat — flagged, not investigated further) and 7 real, recent, visitor-facing Facebook events missing from the site (excluding ~60 pre-2020 founding-era membership-recruitment events, a different category by design): two Star Gazing Camps, Volunteer Day: Chop & Drop, Introduction to Permaculture & Community Living, Young Naturalist Overnight Camp, 3Bs&1H – 5th Edition, and Forage & Feast: Camp Night (the first run of the same format as the already-published 2026 foraging-day draft, same organizer). Opened each event on Facebook individually to pull its real description, schedule, pricing, and facilitators, then wrote one content file per event (`src/content/events/`) matching the site's existing past-event style — full operational detail preserved as a historical record, not rewritten into retrospective narrative, consistent with how already-published past events do it. Added a new `stargazing` tag. No cover images set — no local photo files exist for these yet. Verified with `astro build` (12 → 19 events) and in-browser (listing sort/count, tag filters, table rendering, the interest widget on a new past event). | `0bef248` |
| 2026-07-26 06:15 | Add "Want this to happen again?" interest capture to past events | Sharath Jeppu | "Users browsing past events may be interested in the event being organized again or be a part of the next event. Can we have a way to capture this interest which we can funnel into a demand pipe? ... visible to other users visiting the website as well as the farm event management team." Follow-ups during design: "can this ensure one click per user?" (answered: not with a hard guarantee — no accounts/login exist anywhere on the site; real dedup needs an identity, which only works if an email is given), then "Optional email, but not required," then "If we actually host an event based on the demand, how do we reset the count?" | New capability the site didn't have: showing a count back to *other* visitors needed a publicly-readable store, which nothing here provided (Netlify Forms, used for `/contact` and `/visit/host-an-event`, is write-only). Added a second Netlify Function, `netlify/functions/event-interest.mts`, backed by Netlify Blobs (a public per-event `{count, emails[]}` record) — a one-click "I'm interested" widget on every past event page (`src/pages/events/[slug].astro`, gated on the same `isUpcoming` boolean that already gates `<AddToCalendar>`) shows the live count to everyone, and — only when an email is given — also fires an AJAX Netlify Forms submission (same pattern as the other two forms) so the farm team gets an actionable, contactable entry rather than just a number. Dedup: a `localStorage` flag per browser (not a hard guarantee) plus server-side email-based dedup when one's provided. For resetting once demand is acted on: `netlify blobs:delete event-interest <event-id>` (a built-in Netlify CLI command, no new code) zeroes a specific event's count, paired with a new optional `interestNote`/`interestNoteHref` field on the `events` collection schema (`content.config.ts`) that replaces the widget with a static resolution message instead of silently vanishing. Added `@netlify/blobs`, which pulled in a moderate transitive vulnerability (GHSA-8988-4f7v-96qf, unbounded memory allocation in `@opentelemetry/core`'s Baggage-header parsing, hard-pinned to a vulnerable version by `@netlify/otel`) — fixed via a `package.json` `overrides` entry rather than downgrading; `npm audit` now reports 0 vulnerabilities. Verified locally with `astro dev --background` + `netlify dev` (required for Functions): count GET/POST behavior, case-insensitive email dedup, independent per-event counts, and the `localStorage`-driven already-registered state surviving a reload, all via curl and in-browser. Found and fixed a real bug during that testing — `.interest-form`'s `display: flex` was beating the browser's default `[hidden]` rule (author styles always win over the user-agent stylesheet), so the form never actually hid after a successful submit; same class of bug already fixed once in this codebase for `.header-weather[hidden]`, same fix applied here. Also corrected `ARCHITECTURE.md`'s Host an Event status, which still said "not yet deployed" despite having been pushed to `main` earlier in this session. | `523074a` |
| 2026-07-26 05:38 | Add a "Host Your Event" page, pitching TVC as a venue for outside groups | Sharath Jeppu | "I want to create a section on the website that provides information and engagement opportunity for people or organizations who are looking for such places [to host events]... Can you propose a way to provide this hook on the website?" — confirmed standalone page, mixed nature/tech audience, Netlify Form inquiries, a 5th homepage rung, no on-page pricing. Then, after seeing the built page: "The workshops and retreats are also events. Then there is Team & group events. shouldn't they be clubbed together as they are the same?", then "who conducts the events is not material for the audience... Can all the three be combined into one entry point... It should also provide the option of other types of events that we may not have thought of." | New standalone page at `/visit/host-an-event`, grounded entirely in facts already established elsewhere on the site (98-acre hillslope, 31.8 μcd/m² dark skies, up to 20 people overnight, honest BSNL WiFi/solar+generator framing) rather than invented claims, with two real proof points linked from the events collection — the recurring Dusk to Dawn biodiversity walks, and The Lambda Retreat (a week-long external computation retreat). Closes with a Netlify Forms inquiry form (same working pattern as `/contact`'s "Friends of TVC" signup) instead of committing to pricing. Wired into the site's IA: `ENGAGE_LINKS` in `site-map.ts` (flows to Nav's dropdown and breadcrumbs), and a 5th rung on the homepage's "levels of engagement" ladder, deliberately breaking its prior "4 curated rungs" pattern since this is a revenue goal. Then, per feedback that "who runs it" is a distinction visitors don't care about: consolidated `/visit`'s three overlapping cards (Team & Group Events, Workshops & Retreats, Host Your Event) into one "Events at TVC" entry point, and broadened the page itself — renamed "Who this is for" to "What's possible here," folded TVC's own workshop topics and team-outing facilitation into the existing tracks, and added a third open-ended "Team offsites & anything else" card so the page invites event types not yet listed rather than implying only two tracks exist. Updated `ARCHITECTURE.md`'s Netlify Forms section to reflect the second form. Verified with `astro build` and in-browser at desktop and mobile widths, including an end-to-end form submission smoke test. | `048f69e`, `1a3aa09` |
| 2026-07-26 05:04 | Update astro to 7.1.3, clearing the Dependabot alert | Sharath Jeppu | "check the dependabot vulnerability" | GitHub's alert (GHSA-4g3v-8h47-v7g6) is a medium-severity reflected XSS in Astro's View Transitions animation-property serialization, affecting astro 2.9.0–7.0.9 (the installed 7.0.7), fixed in 7.1.0. Checked exploitability: not reachable here — `astro.config.mjs` has no `output: "server"`/adapter (the site is fully static) and nothing in the codebase uses View Transitions (`transition:animate`, `astro:transitions`, `<ClientRouter>`) at all. Updated anyway since it's a trivial same-major bump already within `package.json`'s existing `^7.0.7` range. `npm audit` now reports 0 vulnerabilities; verified with `astro build`. | `f3c1b01` |
| 2026-07-26 05:00 | Fix event/outreach card images not reaching the card's right edge | Sharath Jeppu | "why are teh pictures in the event car not filling their tile width? Is that deliberate?", then after the first attempt — "I still don't see it fixed. If you see the past events, while the text fills up teh card, the image has a padding on the right" | Not deliberate; took two passes to find the real cause. First pass (`153871c`) fixed a stale `-24px`/`calc(100% + 48px)` negative-margin bleed that no longer matched `.card`'s padding (moved to 28px back in `825de65`) — necessary but not sufficient. The user's follow-up ("padding on the right" specifically, not both sides) pointed at the actual bug: `global.css`'s sitewide `img { max-width: 100% }` was clamping `width: calc(100% + 56px)` right back down to 100%, since `max-width` wins over `width` — the negative margin still pulled the image's *left* edge out correctly (margins aren't affected by `max-width`), but the image's own box couldn't stretch far enough to reach the *right* edge too. Added `max-width: none` to `.grid img` in both `events/index.astro` and `people/community-outreach/index.astro`. Verified with `getBoundingClientRect()` before/after: right-edge gap was 57px, now 1px (matching the left edge, both just the card's own border) — confirmed visually too. | `153871c`, `a64dc16` |
| 2026-07-26 04:53 | Fix fixed-hero background bleeding through the events filter bar and In Pictures page | Sharath Jeppu | "Can you check for UI issues across the website. I think event cards on the evnts page has issues in the mobile view", then "Perform a full UX audit" | Ran the dev server and drove it with the Chrome browser tools at mobile viewport widths across every page (plus a source sweep for the same pattern site-wide). The event cards themselves were fine; the real bug was one section up — the "Filter by type"/"Length" chip bar between Upcoming and Past on `/events`, and the entire `/in-pictures` page (From the archive, filters, top of the photo grid) both sat directly on `PageHero`'s fixed, fading background photo without the opaque background every other in-flow `.section` gets from `global.css` (the same protection added for this in the "Pin every page hero's photo behind the page" change) — so the pinned photo showed through behind the UI for however much of each fell within the first viewport-height of scroll. Grepped every `PageHero`-with-image page for the same missing-background pattern and confirmed these were the only two instances; everywhere else the content is already correctly wrapped in `.section`. Gave `.controls` on `/events` an explicit `background: var(--tvc-cream)`, and wrapped `/in-pictures`' content in `.section wrapper` instead of a bare `.wrapper`. Also checked: mobile nav menu (opens/scrolls fine), the biodiversity map's Gallery/Map toggle (renders fine on mobile), and console errors on several pages (none). Verified with `astro build` and visually in-browser before/after. Confirmed with the user that the floating chat button overlapping trailing text/images is expected and not something to fix. | `bcdd8e7` |
| 2026-07-26 04:40 | Remove orphaned history-page hero image and unused playwright dependency | Sharath Jeppu | "go through the entire website and clean up what is redundant" | Audited the repo for genuinely unreferenced/unused material (components, data files, utils, npm dependencies, public assets, CSS) rather than restyling anything that was still in use — most of the codebase turned out to already be single-purpose and well-referenced. Found and removed two concrete leftovers: `public/images/pages/history/hero.jpg` (255KB, from the pre-rename "history" page in the initial Astro rebuild, unreferenced since it became `/our-journey`), and the `playwright` devDependency (added in one commit for an ad-hoc visual check, never used in any script/config/test since). Verified with `astro build` afterward. | `41675bd` |
| 2026-07-25 22:29 | Pin every page hero's photo behind the page, fading on scroll | Sharath Jeppu | Asked what the homepage's scroll effect was called, then "Can this be replicated across all the pages?", then narrowed to "Can the hero image be fixed and not roll up and fade as i scroll up on every page?" — tried a no-fade pin first per that ask; found in-browser that it either bled the pinned photo through ordinary page content or (once the hero was made tall enough to avoid that) left the hero's own white text illegible once the image had to be hard-hidden. Presented both findings and a recommendation each time; user picked the safe/proven option both times, landing on "bring back the fade" — i.e. replicate the homepage's actual working effect. | `PageHero.astro`'s image variant now pins its photo as a `position:fixed` layer behind the whole page (previously scoped inside the hero's own ~420px box, scrolling away like normal content), fading it and its scrim out over the first viewport-height of scroll — the identical technique and easing `index.astro`'s own hero already uses, applied to the ~20 pages that route through `PageHero`. Added `background: var(--tvc-cream)` as `.section`'s new default in `global.css` — invisible under normal use (identical to the previous transparent-over-cream-body look) but what actually stops the fixed, still-fading photo from bleeding through a page's own content, the same reason the homepage's `fork-section` needed an explicit background. Verified via `npx astro build` (34 pages, clean) and in-browser at desktop and mobile widths across About, Visit, Our Journey, Privacy (plain hero, confirmed unaffected), and Home (confirmed unchanged): photo fades correctly, no bleed-through at any scroll depth, re-fades in correctly scrolling back up, no console errors. | `9286358` |
| 2026-07-25 22:02 | Merge the Timeline into Our Journey, re-theme it to match the site | Sharath Jeppu | "Merge the timeline and our journey page into a single page. Also, the timeline page is not design coordinated with the rest of the website. fix it and ensure it still look as cool" — followed by "remove the early days section completely. It does not fit well" and "There is a lot of white space under the hero banner" after an initial pass. | The year-by-year timeline (`JourneyTimelineStandalone.astro`) lived at a separate `/our-journey/timeline` URL behind a click-through link; deleted that page and render the component directly on `/our-journey` below the hero instead. Removed the "Early days" text section per follow-up feedback, so the page now flows straight from the photo hero into the timeline's own poem intro. Re-themed the timeline's CSS from its bespoke near-black palette to the site's real `--tvc-*` tokens (cream ground, ink text, green/orange accents), keeping the full-viewport scrollytelling year sections, fanned photo carousel, and lightbox intact — text sitting directly on a photo (carousel cards, the lightbox) still uses light text on a dark scrim, the same convention `PageHero`'s image variant already uses site-wide. Fixed two bugs the merge itself introduced: the fixed sidebar index and progress rail (designed for when the timeline was the whole page) were leaking over the hero/intro content above them, now gated to only show once the timeline section fills the viewport (checked via the viewport's own center point crossing `.jt-root`'s bounds, refined to require the section fully cover the screen after an initial version still showed the chrome too early on tall viewports); and the poem intro's `min-height: 100vh` — sized for when it was the page's own opening screen — left a large dead gap of empty cream under the hero once "Early days" was removed, fixed by sizing it to its own content. Old URLs (`/timeline`, `/our-journey/timeline`) now redirect straight to `/our-journey`. Verified via `npx astro build` (34 pages, clean), in-browser at desktop and mobile widths (sidebar/rail activation, carousel, lightbox, no-photo cards), and confirmed both redirects return 301 to `/our-journey`; no console errors. | `cf4c543` |
| 2026-07-25 21:37 | Sharpen the biodiversity grid's blurry photo tiles | Sharath Jeppu | "the picture tiles on the biodiversity page is unclear and blurred unlike how it renders on the inaturalist page. fix it" | `thumbUrl()` in `observation-utils.ts` was swapping iNaturalist's photo URL to the "small" (~240px) size for grid cards — fine in isolation, but the grid's cards render at 210-300px CSS width, which on any retina/high-DPI screen needs roughly double that in actual source pixels to look sharp, so the images came out visibly soft compared to iNaturalist's own site. Switched to "medium" (~500px), the same size the detail panel's own larger photo view already used and rendered sharp. Verified in-browser on `/ecosystem`: grid photos (turtle, plants, reptiles) render crisp under zoom, a production build still completes cleanly (35 pages), no console errors. | `3bbfab4` |
| 2026-07-25 21:24 | Fold Partners into /people, simplify the Community Outreach index | Sharath Jeppu | "There are still sections or pages that are hidden. Like the partners. Logically this probably does not need a seperate page. Can you identify more such anamolies and provide recommendations" — checked word counts and inbound-link reachability across the site and reported two anomalies (Partners, Community Outreach), confirming Visit sub-pages, Events, Ecosystem/Weather, In Pictures, and Resource Centre were all fine as-is. User approved with "Yes, go ahead and implement this." | Each of the 5 partners (`A Rocha India`, `Ananas`, `Biome Solutions`, `Linger`, `WeCommunities`) carried only 88-130 words, so the dedicated `/people/partners` listing + 5 individual `/people/partners/[slug]` routes were more scaffolding than the content warranted. Added a required `url` field to the `partners` content schema, deleted both partner page files, and replaced the old "Partners & collaborators" teaser on `/people` with an inline card grid (`id="partners"` anchor) linking each partner straight out to its own site — descriptive prose paragraphs were kept in each partner `.md` file for potential future re-expansion, just no longer rendered as a standalone page. Community Outreach has only 1 post today, which rendered as an awkward "grid of 1" card; `people/community-outreach/index.astro` now conditionally renders that single post's content directly inline via `render()`/`<Content />` when there's exactly one entry, falling back to the original grid once there are 2+. Hit an `InvalidRedirectDestination` build error since Astro's `redirects` config can't send a dynamic source route to a static destination — moved the two per-partner-slug redirects (`/ecosystem/partners/*`, `/people/partners/*` → `/people#partners`) into `netlify.toml` as wildcard rules instead, keeping the static `/ecosystem/partners` and `/people/partners` redirects in `astro.config.mjs`. Verified with `npx astro build` (35 pages generated cleanly, correct `noindex`/canonical redirect stubs) and in-browser on the dev server: `/people#partners` shows all 5 partner cards with logos/excerpts, `/people/community-outreach` shows the STEM Experiential Learning Classes post inline under its own heading instead of as a click-through card, no console errors on either page. | `d17605e` |
| 2026-07-25 19:10 | Reorganize site content: new People section, trimmed About/Journey | Sharath Jeppu | "Need to reorganize the content sections. for example our journey has design and community elements which are out of place and probably need to be under about TVC. Similarly, the members and other people related section perhaps need a separate section probably titled 'people'. Could you go through the current structure and recommend what needs reorganization." Presented a full site-structure review with a proposed reorg; user approved with "Yes, go ahead and implement this." Mid-implementation, also asked to simplify 3 of the 4 homepage stat tile labels to one word. | Went through every page and content collection to map the actual IA before touching anything. Split "who's part of TVC" out of About TVC and Our Journey into a new `/people` page: Member families and Local staff (moved from About), Partners (moved from `/ecosystem/partners` — never had anything to do with the ecosystem topic beyond URL convenience), and Community Outreach (moved from `/our-journey/community-outreach` — it's about TVC's relationship with the neighboring village, not TVC's own history; telling sign was that Resource Centre already linked to it as a general resource, not a "journey" page). Also moved The Design from Our Journey to `/about/design`, since it's a present-tense description of the farm's current layout, not a historical milestone. About TVC is now identity-only (Vision/Mission/Farming/principles) plus teasers to Design and People; Our Journey is now purely the historical narrative (Early days + Timeline). Added `/people` to the Explore nav dropdown and to `EXPLORE_PREFIXES` for breadcrumbs. Added redirects for every moved URL — including dynamic partner/outreach slugs — in `astro.config.mjs`, following the exact pattern an earlier reorg had already established there, pointing straight at final destinations rather than chaining through intermediate stops (e.g. the pre-existing `/community-outreach` shortcut now points directly at `/people/community-outreach` instead of via `/our-journey/community-outreach`). Simplified 3 of 4 `HomeStats.astro` tile labels to one word (Years, Families, Species — Acres already was), and updated the "Families" tile's link from `/about#members` to `/people#members`. Verified thoroughly: dev server confirmed every new route returns 200 and every old route 301-redirects correctly (including dynamic slugs, tested with a real partner and outreach post); a production build generated all 41 pages cleanly with proper `noindex`/canonical tags on the generated redirect HTML; visually walked through People, About, Our Journey, a partner detail page, and the Design page confirming breadcrumbs, eyebrows, and cross-links; no console errors. | `6a3d165` |
| 2026-07-25 18:39 | Fix hero text overlapping the stat tiles on narrow phones | Sharath Jeppu | "The render on my iphone (15 Pro Max) have the text in the hero banner and the tiles overlapping." | This browser environment couldn't reliably render below ~500px width to reproduce the exact iPhone viewport, so tracked it down by simulating the real content width (382px, matching a 430px iPhone screen minus wrapper padding) via direct DOM measurement: `.hero__content` (heading + intro) wraps onto more lines at that width, and centering it within `.hero`'s fixed 640px box left as little as ~3px of clearance above the stats bar overlaid at the bottom — indistinguishable from true overlap on a real device, which is exactly what got reported. In `index.astro`, added a `max-width: 640px` breakpoint giving `.hero` a taller `min-height` (700px) and a `padding-bottom` (160px) that reserves guaranteed space for the stats bar, shrinking the region the heading/intro get centered within rather than relying on a fixed height that doesn't adapt to however many lines they wrap to. Verified via the same DOM-measurement approach post-fix: gap at the simulated iPhone content width went from ~3px to ~113px; confirmed desktop (1440px, unaffected by the mobile-only media query) renders unchanged; no console errors. | `db51468` |
| 2026-07-25 18:21 | Replace "100 acres" with a boundary-derived 98 acres, site-wide | Sharath Jeppu | "can you calculate the area of the farm based on the boundary on Open Street Maps?" then "Link this number to all mentions of areas across the website. Round it to the nearest integer" | Calculated the property's area from the TVC boundary polygon mapped on OSM (`tvc-boundary.ts`): a geodesic spherical-excess calculation and an independent equirectangular-projection shoelace calculation agreed to within a few square meters — 397,971 m² = 98.34 acres, rounded to 98. Added a single shared constant (`FARM_AREA_ACRES` in new `src/data/site-facts.ts`) and pointed every current, present-tense mention of the farm's size at it: homepage hero/meta/stat tile, About's hero/body copy, the footer blurb, 404 copy/meta, Our Journey's body/meta (+ its timeline sub-page), Join's "on X acres" line, and `BaseLayout`'s default meta description + JSON-LD Organization description. Deliberately left past event pages (2021–2024, several already stale on member count too — "52 families" never updated to 53) and the founding-story membership/acreage target in `JourneyTimelineStandalone.astro` untouched, since those describe the plan or copy as it stood at the time, not a present-tense claim about the property's current size. Verified in-browser across homepage, About, 404, Our Journey, Join, and the footer — all show 98/98-acre correctly; confirmed a historical event page still reads unchanged ("100-acre... run by 52 families"); no console errors. | `77ae4d1` |
| 2026-07-25 16:49 | Overlay homepage stats on the hero, with a fading fixed background | Sharath Jeppu | Iterative, over several turns: "Can [the stats] be over layed on the hero banner at the bottom. I would like them each to be in a translucent box" → "Use the effect that Apple creates" (glass tiles) → "link the tiles to their respective content pages" → "there is no indication that tiles are clickable when the mouse hovers over it" → "centre the tiles in the mobile view" → "can the picture also fade out as the rest of the content from the bottom come up" → "The dissolve is too fast and not pleasing" → finally: "don't fade the header content. let the whole page float over the background picture which should slowing fade as one scrolls down" → "The text in the second section is not visible over the picture while scrolling." | `HomeStats.astro`'s four stats (Acres / Years regenerating / Member families / Species observed) now render as frosted-glass tiles (`backdrop-filter: blur + saturate`, an inset highlight for a lit-edge look, Apple's "material" style) overlaid on the bottom of the homepage hero image instead of a separate section below it; each tile links to its relevant page (`/about`, `/our-journey`, `/about#members`, `/ecosystem`) with a bold hover state (orange border, brighter glass, lift) and is centered symmetrically on mobile with clearance reserved for the fixed chat bubble. Went through a full scroll-jacked "pin and roll up" Apple Vision Pro-style effect first, then rebuilt it simpler per explicit direction: in `index.astro`, the hero picture is now a `position: fixed` backdrop behind the *entire* page (not just the hero) that fades out on an eased curve over roughly one viewport height of scroll, while the heading/intro/stats — and every section after them — are normal, unanimated content that just scrolls over it. Gave the next section ("Explore, or engage") its own opaque background, since it's transparent by default and briefly had unreadable dark text sitting directly on the still-fading photo. Verified in-browser across the whole arc at desktop/mobile widths: tiles link correctly, hover/glass effect renders, mobile centering measured symmetric (76px both sides), content stays static while only the picture fades, next section is always legible, no console errors at any step. | `5c72fea` |
| 2026-07-25 01:43 | Show live temperature in the header; rename "Geography & Weather" to "Weather" | Sharath Jeppu | "Show the weather on the header and slicking [clicking] it should land on the Geography and Weather Page. Geography and Weather sounds wordy. Change this to only weather as there is very little about geography on this page." Clarified mid-task: "Show the temperature. Not weather." | New `HeaderWeather.astro` renders a compact icon+temperature pill next to the logo, site-wide (via `Nav.astro`), linking to `/ecosystem/geography` — just the reading itself, no text label, per the follow-up clarification. Extracted the fetch/cache/format logic shared with `WeatherWidget.astro` into `utils/weather.ts` so both draw from the same 15-minute localStorage cache instead of duplicating the Open-Meteo call (mirrors the existing `observation-utils.ts` pattern for biodiversity data). Hidden below the nav's own 860px hamburger breakpoint — the mobile header is already tight, and the page is one tap away via the menu. Separately, renamed "Geography & Weather" to "Weather" everywhere it's referenced: `site-map.ts` (nav dropdown + breadcrumb section), the page's own title/hero/meta description, and both places that link to it (the homepage's Explore card and the Biodiversity page's card). Left the on-page "Geography & landscape" section heading and its paragraph as-is — the rename was about the page's name being wordy, not about cutting the actual geography content. Verified in-browser at 1440px, 900px, and 390px: pill shows the live reading matching the widget on the page itself, clicking it lands on `/ecosystem/geography` with breadcrumb "Home / Explore / Weather", hidden correctly below 860px, mobile menu and homepage/biodiversity cards all read "Weather", no console errors. | `f579e8b` |
| 2026-07-25 01:33 | Show location and observation time on the weather widget | Sharath Jeppu | "The temperature on the weather page does not indicate the location or the time that this is for. Can that be added?" | `WeatherWidget.astro` on Geography & Weather now shows a "TAMARIND VALLEY COLLECTIVE" label above the reading and "As of H:MM AM/PM IST" below it, using Open-Meteo's own `current.time` rather than the visitor's device clock (which would just show whenever their browser rendered the page, not when the reading was actually taken). That timestamp comes back as a naive "YYYY-MM-DDTHH:MM" with no UTC offset — already the farm's Asia/Kolkata wall-clock time, since that's what the API's `timezone=` request param controls. Deliberately parses the hour/minute out of the string directly rather than via `new Date()`, which would otherwise silently reinterpret those same digits as the *visitor's own* local timezone and show the wrong hour to anyone outside IST. Verified in-browser at desktop and mobile widths: displayed "As of 1:30 AM IST" matched the live API response's `current.time` of "2026-07-25T01:30" exactly, no console errors. | `39f5e5a` |
| 2026-07-25 01:27 | Align the Explore and Engage nav dropdowns consistently | Sharath Jeppu | "Teh explore sub menu and the engage sub menu align differntly. fix it" | The two dropdowns used opposite CSS anchors — Explore left-aligned to its own trigger, Engage right-aligned (added in an earlier fix to stop it overflowing off-screen once it also became a two-column menu) — so they visibly hung in different directions off their triggers. In `Nav.astro`, switched both to centering under their own trigger (`left: 50%` + `transform: translateX(-50%)`), the one rule that reads the same for every item. Centering alone still clips an edge item at desktop widths narrow enough to be tight but not yet mobile (~861–950px), so added a small script that measures each dropdown on hover/focus and nudges it back on-screen via a `--dropdown-shift` CSS variable only when needed. Also had to explicitly reset `transform: none` in the mobile breakpoint — unlike `left`/`right`, `transform` isn't neutralized by `position: static`, so without the reset the centering translate would have shifted the mobile stacked menu sideways. Verified in-browser at 1440px (both centered, matching), 900px and 870px (both fit on-screen, nudged left as needed, re-verified after ruling out a mouse-path testing artifact where a fast hover transit briefly showed the wrong dropdown), and 390px mobile (unaffected, still stacks flush left), no console errors. | `9e6e443` |
| 2026-07-25 01:16 | Restrict biodiversity sightings to the TVC property boundary | Sharath Jeppu | "Can the biodiversity page be restricted to only sightings within the TVC boundary as shown in the Open Street maps?" | Looked up TVC's actual mapped boundary on OpenStreetMap (way `1522934798`, tagged `leisure=nature_reserve`, fetched via Nominatim's `polygon_geojson` export) and checked it against the live iNaturalist project data first: of 169 fetched observations, 24 (~14%) sit outside that polygon — mostly sightings from the adjoining Thaggatti Reserve Forest, plus a couple of far-off outliers (a sloth bear several km away). Snapshotted the 217-point boundary into `tvc-boundary.ts` and added a ray-casting point-in-polygon test (`isWithinBoundary` in `observation-utils.ts`) — observations with no coordinates at all (location hidden by the observer for a sensitive species) are excluded too, since they can't be verified as on the property. Applied everywhere `BiodiversityExplorer.astro` derives from the fetched set (stats, chips, gallery, map, leaderboard) and in `HomeStats.astro`'s independent "species observed" fetch, so the homepage number can't drift from what `/ecosystem` itself shows. Also drew the boundary outline on the Leaflet map for context, and swapped its empty-state fallback view (previously a hardcoded point/zoom) for the boundary's own bounds. Verified in-browser at desktop and mobile widths: observation count dropped from 169 to 143 (169 minus 24 outside minus 2 with no taxon ID), homepage "Species observed" now matches `/ecosystem` exactly (134, down from 150), all markers render inside the drawn boundary outline including two near a narrow southern spur, popups still open correctly, no console errors. | `c796fa9` |
| 2026-07-25 01:06 | Fix inconsistent nav dropdown columns + a mobile-menu overflow bug | Sharath Jeppu | "The menu on the top is inconsistent. One is a single column and the other is 2 columns." | `Nav.astro`: both `EXPLORE_LINKS` and `ENGAGE_LINKS` have grown to 7 items each, but only Explore was flagged `wide: true` for the two-column dropdown treatment — Engage still rendered as a tall single column. Set Engage `wide: true` too, which then overflowed past the right edge of the viewport since the dropdown was still anchored `left: 0`; added `.nav__item:last-child .nav__dropdown { left: auto; right: 0; }` so it grows back toward the menu instead. Asked before going further, then (with confirmation) also fixed a pre-existing, unrelated bug found while testing on mobile: the open hamburger menu inherited `flex-wrap: wrap` and `justify-content: flex-end` from the desktop row-layout rules, uncleared in the `@media (max-width: 860px)` block — with two 7-item dropdowns now taller than the menu's capped `max-height`, that split Explore/Engage into two side-by-side columns and pushed overflowing content upward past the container's top edge, unreachable since `scrollTop` can't go negative. Reset both to `nowrap`/`flex-start` in the mobile block. Verified in-browser: both dropdowns render as two columns and stay on-screen at 1440px, the mobile menu opens as one scrollable stack from "Explore" down through "Contact Us" at 500px, no console errors. | `30cc519` |
| 2026-07-25 00:55 | Make the footer more compact | Sharath Jeppu | "Can the footer be made more compact" | `Footer.astro` was a single centered column stacking brand, blurb, social links, copyright, and legal links with generous padding/gaps — tall on every screen size regardless of the wide desktop viewport available. Split it into two rows: a top row placing brand, blurb, and social links side by side (wraps to a centered stack under 640px), and a slimmer bottom bar (with a subtle divider) for copyright and legal links. Reduced padding, gaps, and font sizes throughout. Caught and fixed a real bug while testing on mobile: a `flex: 1 1 320px` meant to set a minimum row-width on desktop became a 320px minimum *height* once the layout switched to `flex-direction: column` under 640px, leaving a large blank gap under the blurb — reset to `flex: 0 1 auto` in the mobile breakpoint. Verified in-browser at both desktop (1440px) and mobile (390px) widths: roughly half the previous height on desktop, no blank-space bug on mobile, cookie-preferences button still opens the consent banner correctly, no console errors. | `b9d8a23` |
| 2026-07-25 00:49 | Hide the Aug 1 Foraging Day event | Sharath Jeppu | "hide the event foraging day on Aug 1 as it has been postposed indefinitely" | Set `draft: true` on `src/content/events/2026-08-01-foraging-day.md`'s frontmatter — the events collection already filters out drafts on both `/events` and its own detail page (`e.data.draft`), the same mechanism the `_template.md` file documents for keeping unpublished events invisible. Content and images left in place in case the event is rescheduled. | `a424212` |
| 2026-07-24 18:54 | Add site-wide scroll-reveal for .section elements | Sharath Jeppu | Fourth and last of four in this round. | `ScrollReveal.astro`, rendered once from `BaseLayout` so every page gets it with no per-page wiring — `.section` is already the common structural class nearly every page uses (confirmed neither `JourneyTimelineStandalone` nor `BiodiversityExplorer` use it, so their own custom scroll effects are untouched). Hidden state only applies once JS has actually run, so content stays visible by default with JS disabled/slow; respects `prefers-reduced-motion`. Found and fixed a real bug during testing: `IntersectionObserver` only fires on a detected intersection *change* between rendered frames, so a fast scroll (reproduced with a 30-tick wheel scroll) can carry a section through the viewport without ever rendering a frame where it intersected, leaving it stuck invisible even though the visitor scrolled past it — added a scroll-event safety net that reveals anything whose top has reached the viewport. Verified in-browser on three page shapes (homepage, About's long member grid, the Journey timeline's custom scroll design): immediate reveal for already-visible sections, scroll-triggered reveal below the fold, the fast-scroll bug fixed (confirmed all 5 homepage sections revealed after one 30-tick scroll, not just 1), timeline page unaffected, no console errors. | `0ed3dd3` |
| 2026-07-24 18:44 | Add a live weather widget to Geography & Weather | Sharath Jeppu | Third of four in this round. | `WeatherWidget.astro` fetches current temperature/humidity/conditions for the farm's own coordinates (12.35, 77.65 — already used elsewhere for the biodiversity map's default view) from Open-Meteo, free and no API key required, cached in `localStorage` for 15 minutes (same pattern as `BiodiversityExplorer`'s iNaturalist cache); hides itself if the fetch fails. Updated `ARCHITECTURE.md`'s diagram, browser-layer detail, and status table for the new external service. Verified in-browser: real current conditions rendered correctly, confirmed the cache actually prevents a second network call on reload (zero `open-meteo.com` requests on a cached reload, `fetchedAt` timestamp unchanged), mobile layout, no console errors. | `3aefb9e` |
| 2026-07-24 18:37 | Add "From the archive" (on this day) photos to In Pictures | Sharath Jeppu | Second of four in this round. Checked the actual photo dates before building: a strict same-day match would be empty ~91% of the year (only 31/365 days have any match at all across 195 photos) and 114 of them carry a synthetic fallback date rather than a real one — flagged this and asked how to proceed; user picked widening to a +-7 day window. | `OnThisDay.astro` matches photos with real camera EXIF (excluding the synthetic `YYYY-MM-01` fallback timestamps assigned during curation to photos with no real date) within 7 days of today's month/day across years, placed above the main gallery on `/in-pictures`; hides itself when nothing matches (still ~35% of days). Verified in-browser against the actual date math computed beforehand: today surfaced exactly the two predicted photos (both dated 29 July 2017), links open the full-resolution image, mobile layout, no console errors. | `9a24627` |
| 2026-07-24 18:31 | Add a "what's next" event banner to the homepage | Sharath Jeppu | Asked for more engagement recommendations after saying no to member testimonials (no real testimonial content exists); presented four more ideas that reuse existing data only, picked all four via a multi-select. First of four in this round. | `WhatsNextBanner.astro` surfaces the next upcoming event (title, date, countdown, link) right after the homepage stats strip — the Events page already computes this same countdown, but it was only visible to someone who clicked through there. Reuses `src/utils/events.ts`'s existing helpers so the countdown text can't drift from the Events page's own. Renders nothing when there's no upcoming event. Verified in-browser: correct event/date/countdown matching the Events page's own "8 days to go", mobile layout, no console errors. | `15e5e15` |
| 2026-07-24 18:23 | Add quick-prompt suggestion chips to the chat widget | Sharath Jeppu | "convert interest into action" — second of the two features in this group (the first was "Add to calendar," row above). | Three tappable questions ("How do I visit?", "What's the next event?", "How does membership work?") appear once under the welcome message on first open, in `ChatWidget.astro`. Refactored the form submit handler's inline send logic into a shared `sendMessage()` so both a chip click and a typed message go through the same path and either one permanently dismisses the suggestion row for that session. Verified in-browser: chips render on first open, clicking one removes the row and sends that question through the normal flow (confirmed via the graceful "couldn't reach the server" fallback, since local `astro dev` doesn't serve the Netlify function the way `netlify dev` does), suggestions don't reappear on close/reopen, typing a custom message also dismisses them, no console errors. | `e2e5612` |
| 2026-07-24 18:19 | Add "Add to calendar" to upcoming event pages | Sharath Jeppu | "convert interest into action" — picked from the remaining four engagement recommendations (the other three were hero hotspots/before-after-slider, already undone, plus member testimonials and chat quick-prompt chips). First of two in this group. | Everything generated at build time, no client JS for the links themselves: `src/utils/calendar.ts` builds a Google Calendar URL and a `data:` URI `.ics` file from an event's title/excerpt/date/endDate, correctly converting the schema's inclusive `endDate` to ICS's exclusive `DTEND` and RFC 5545-escaping the description/location. `AddToCalendar.astro` is a small click-to-open dropdown (Google Calendar / Apple-Outlook `.ics`) wired into the event detail page, gated to upcoming events only — adding a past event to your calendar is a dead end, not a conversion. Verified in-browser: menu opens with both options, decoded the actual generated URLs from the built HTML output (correct exclusive end-date range, correctly escaped fields, valid UID), confirmed the button is absent on a past event, no console errors. | `5f4b73c` |
| 2026-07-24 18:10 | Undo the before/after slider and homepage hotspot map | Sharath Jeppu | "undo 3 and 4" — the last two of the four prioritized engagement features (see the 17:45 row below for the original selection). | `git revert`ed the four commits that added them (`e8f96d4`, `c09be22`, `91941c5`, `0e0ffd2`) in one pass — deletes `BeforeAfterSlider.astro` and `FarmMap.astro`, and the few lines wiring each into `our-journey.astro`/`index.astro`. Applied cleanly with no conflicts since nothing had been built on top of them. The live stats strip and Biodiversity leaderboard (the first two prioritized features) are untouched. | `1388215` |
| 2026-07-24 17:50 | Add a top-contributors leaderboard to the Biodiversity page | Sharath Jeppu | Second of four prioritized engagement recommendations (see the 17:45 row above for the original proposal/selection context). | Ranks all contributors by total observation count in `BiodiversityExplorer.astro` (medals for the top 3), computed from the full unfiltered dataset so it stays stable rather than jumping around as filters change. Clicking an entry reuses the existing `setContributorFilter()` already wired to the per-card overlay button, so it stays in sync bidirectionally — filtering via a card highlights the matching leaderboard entry and vice versa — and propagates to the map the same way. Verified in-browser: correct ranks/counts, click-to-filter from the leaderboard, active-state sync in both directions, map marker count matching the selected contributor's total, no console errors. | `dc329df` |
| 2026-07-24 17:45 | Add a live stats strip to the homepage | Sharath Jeppu | "Propose a few design changes to the website to make it more engaging" — presented 8 grouped recommendations (hero hotspots, before/after slider, live stats strip, biodiversity leaderboard, member testimonials, scroll-reveal animations, add-to-calendar, chat quick-prompts) without implementing anything; asked which to prioritize via a multi-select question. User picked hero hotspots, before/after slider, live stats strip, and biodiversity leaderboard — building one at a time, validating each locally before committing. | Added `HomeStats.astro`, rendered right after the homepage hero: 100 acres / years regenerating (computed from 2017) / 53 member families are static, species observed is live — reusing `BiodiversityExplorer`'s own `fetchAllObservations()` and its 10-minute `localStorage` cache (instant if a visitor already opened `/ecosystem` this session) and the same unidentified-findings filter. Numbers count up via `requestAnimationFrame` easing when the section scrolls into view (`IntersectionObserver`), respecting `prefers-reduced-motion`. Verified in-browser: correct final values matching the Biodiversity page's own species count, mobile 2-column layout, no console errors. | `2e52195` |
| 2026-07-24 17:28 | Make the Biodiversity map respect the gallery's filters | Sharath Jeppu | "The filters don't seem to get applied on the map view. Fix this" — the map view built its markers from the full, unfiltered observation list on first open and never updated, so neither the taxon chips nor the just-added observer filter touched it. | Added `refreshMap()` to `BiodiversityExplorer.astro`, called after both `setFilter()` (taxon chips) and `setContributorFilter()`: tears down the tracked Leaflet instance and re-initializes from `filteredObservations()`. The first-open path (`view-map` click) now also seeds from `filteredObservations()` instead of the full list, so opening the map with a filter already active shows the right subset immediately. Verified in-browser: first map open with a filter pre-applied, a filter change while the map is already open, taxon + observer filters combined, and clearing both filters restoring the full marker set — marker counts matched the gallery/chip counts at every step. | `889a168` |
| 2026-07-24 17:23 | Add a click-to-filter observer overlay on Biodiversity cards, drop unidentified findings | Sharath Jeppu | "in the inaturalist page, I would like the observer handle to appear as a button overlay on the picture. Clicking on it should filter on contributors of the observer. Clicking again should clear the filter. (or suggest a suitable alternative to clear the filter easily). render this locally first for me to validate the UX" — followed by four refinement rounds after reviewing the local render: move the overlay to the top-left, drop the "@" prefix, show the observer's display name instead of their handle, and also filter out any observation with no identification at all. Confirmed working via `astro dev` + in-browser checks before each "commit and push." | Each observation card's photo in `BiodiversityExplorer.astro` now shows a small overlay button (top-left, observer's display name with a handle fallback) that filters the gallery to that observer's contributions, combining with the existing taxon chips; clicking the same observer again clears it, and a "Showing observations by..." pill with an explicit Clear button is the more discoverable alternative. `.obs-card` had to change from a `<button>` to a `div[role=button]` to host the nested overlay button (buttons can't nest) — keyboard activation is restored via an explicit keydown handler, and the overlay's click handler stops propagation so it doesn't also open the detail panel. Also drops observations with `taxon === null` from every derived view (stats, chips, grid, map), not just the gallery, since an unidentified finding isn't a "what lives here" answer. Verified in-browser at each step, including the unidentified-count drop reflected correctly in the stats/chips (113 → 112). | `b515485` |
| 2026-07-24 16:59 | Add a GDPR cookie consent banner, gate GA behind it | Sharath Jeppu | "add the cookie consent banner for GDPR" — a follow-up to the earlier GA addition, which had loaded unconditionally with no consent mechanism. Asked first whether a linked privacy/cookie policy page should be built alongside the banner (none existed); user confirmed yes. | Google Analytics is now opt-in instead of automatic: `BaseLayout.astro`'s GA loader exposes `window.tvcAnalytics.load()`/`.revoke()` but does nothing on its own; the new `CookieConsent.astro` component (rendered site-wide) shows a banner on first visit with Accept/Reject at equal visual weight, calling `.load()` only on Accept and remembering the choice in `localStorage`. Rejecting — or later withdrawing via a new "Cookie preferences" link in the footer, which reopens the banner without a page reload — calls `.revoke()`, using gtag's own documented `ga-disable-<id>` flag so a script already injected earlier in the session stops sending hits. Added `/privacy`, a short factual page (no legal boilerplate) covering what's actually implemented — GA4, the stateless chat relay to Anthropic, the Friends of TVC signup form, and the third-party services a few pages embed directly — linked from both the banner and the footer. Updated `ARCHITECTURE.md`'s GA entry, diagram, and status table to describe the consent gate. Verified in-browser end-to-end: banner shows on first visit, Accept persists and hides it, a reload doesn't re-show it, Reject persists denial, and the footer link reopens the banner. Caught and fixed one bug along the way — the new `/privacy` page's `PageHero` `title` prop was passed a literal `&amp;` instead of `&`, which rendered as a visible `&amp;` in the heading; this is a repeat of a bug this codebase's changelog already documented once before (component props need a literal `&` since Astro escapes it safely on its own). | `154de71` |
| 2026-07-24 16:21 | Add Google Analytics (GA4) | Sharath Jeppu | "Update the website for google analytics" — no measurement ID was given initially; asked for it and confirmed no existing GA/GTM setup was in the codebase, then received `G-795FTPB47P`. | Added the standard gtag.js snippet to `BaseLayout.astro` (`<head>`, `is:inline`), so every page gets it with no per-page wiring, guarded to skip loading entirely when `location.hostname` is `localhost`/`127.0.0.1` so local dev/testing traffic never pollutes production analytics. Updated `ARCHITECTURE.md`: added GA as a new browser-side node and external service in the Mermaid diagram, a paragraph under Visitor's Browser, and a row in the Current Production Status table. Verified in-browser on the local dev server that the guard works (no injected script tag, no `window.gtag`/`dataLayer` defined) and confirmed via the production build output that `dist/*.html` carries the `googletagmanager.com/gtag/js?id=G-795FTPB47P` script tag and config call. No cookie-consent banner added — noted in `ARCHITECTURE.md` as a candidate to revisit if EU/UK visitor volume becomes significant. | `d677e24` |
| 2026-07-24 16:13 | Widen the iNaturalist observation cache to localStorage | Sharath Jeppu | "do the iNaturalist caching fix too" — the last open item from the image-optimization survey, which had flagged the biodiversity explorer's client-side iNaturalist fetch as uncached on every page load. | That earlier finding turned out to be based on an incomplete grep — `fetchAllObservations()` in `observation-utils.ts` already cached fetched observations for 10 minutes, just scoped to `sessionStorage`, so a closed tab or a second tab still paid the full fetch again within that same window. `BiodiversityExplorer.astro` also carries an explicit prior-decision comment against converting this to SSR/Live Content Collections, since that would need `output: 'server'` and break the site's fully-static hosting — so a server-side cache was never on the table. Switched the existing cache from `sessionStorage` to `localStorage` (same 10-minute TTL, same trimmed payload, same quota-safe try/catch) so it persists across tabs and reopens instead of dying with the tab. Verified in-browser: `localStorage` holds the cache key after first load, and a fresh navigation afterward makes zero requests to `api.inaturalist.org` while still rendering the correct stats and observation grid. | `344f4a8` |
| 2026-07-24 16:08 | Lazy-load below-the-fold card grid images; skip width/height attributes | Sharath Jeppu | "do the width/height and lazy loading fixes too" — following up on two remaining items from the earlier image-optimization survey. | Added `loading="lazy"` to cover images on the Partners grid, both Events grids (upcoming and past), Community Outreach grid, and Trekking Trails cards — all sit below each page's `PageHero` but were missing it while equivalent grids elsewhere (Explore cards, member photos, the biodiversity explorer) already had it. For width/height: audited every `<img>` on the site first and found each one already sits inside a container with explicit CSS `width`+`height` (or `object-fit` filling an absolutely-positioned sized parent) — hero, doors, cards, member photos, trail cards, all of it — so the browser already reserves layout space before the image loads and there's no actual CLS to fix. Flagged this to the user rather than doing it anyway; confirmed via a permission prompt to skip it, since adding the attributes would be functionally inert while requiring either hardcoded per-image dimensions (drift risk across the several content-collection loops) or a build-time image-inspection step for no measurable benefit. | `105511c` |
| 2026-07-24 16:00 | Recompress site images to cut page weight | Sharath Jeppu | "check for optimization oppurtunities on the website setup" — a survey of the Astro setup found `public/images` was 40MB across 234 files served as raw, unoptimized `<img>` tags (a 3.4MB event hero saved as PNG despite being a photo, dozens of poorly-compressed journey/member JPEGs). Asked to prioritize and implement the image work; confirmed via a permission prompt before the bulk in-place rewrite, then again after a bug was found and fixed (an accidental double compression pass) before redoing it. | Re-encoded all 224 JPEGs in `public/images` with mozjpeg (quality 80, same dimensions/filenames) and palette-compressed the 8 flat-color PNGs (map screenshots, brand/partner logos, one line-art diagram) — spot-checked several before/after for visible quality loss, found none. Converted the 2022-08-27 biodiversity walk event's cover from a 3.4MB PNG photo to a 370KB JPEG (PNG was the wrong format for an actual photograph) and updated its one content-collection reference. `public/images` drops from 40MB to ~24MB with zero path/reference changes otherwise. Verified with a full `astro build` (40 pages, `netlify/functions/site-content.json` byte-identical since only image bytes changed). Originally committed on a `design-system-preview` branch; at the user's request that branch (including its large unrelated uncommitted diff, discarded) was deleted locally and on GitHub, and this change was cherry-picked directly onto `main` instead. Left open for a future pass: missing `width`/`height` on `<img>` tags, inconsistent `loading="lazy"` on some cover images, and BiodiversityExplorer's uncached client-side iNaturalist fetch on every page load. | `0074f72` |
| 2026-07-19 | Sync the homepage In Pictures thumbnail | Sharath Jeppu | "The thumb nail still shows the old image" — the homepage's Explore card for In Pictures still linked to the pre-swap bare-land photo after the previous change updated the page's own hero. | `index.astro`'s `exploreCards` entry for In Pictures still hardcoded the old 2017 photo path. Updated it to the same tree-climbing photo now used on the page's hero, matching the pattern every other Explore card already follows (teaser image = destination page's own hero). Verified in-browser. | `c7a88d4` |
| 2026-07-19 | Swap the In Pictures hero photo | Sharath Jeppu | "I don't like the hero banner on the section In Pictures. Can you check what can be it replaced with to make it more engaging?" — a photo-mosaic hero redesign was mocked up and rejected ("I don't like this"), clarified to "I just want to change the picture on the top of the page." Asked for "a more colourful image with a flower or bird" next (no bird photo exists in the curated set, so a sunflower shot was offered instead), then picked a specific one by title: "choose the one titled Climbing trees is something that kids miss in cities." | Iterated on mockups (Artifact pages using the site's real photo URLs and colors) before touching any code, including a rejected full mosaic-hero concept. Once the ask was clarified to a simple photo swap, updated `in-pictures.astro`'s `PageHero` (and matching `BaseLayout` OG image/dimensions) from the old bare-land 2017 photo to "Climbing trees is something that kids miss in cities" (2024-08-18), verified in-browser via the local dev server. | `b08b54d` |
| 2026-07-19 | Curate first 81 In Pictures photos, add AI-drafted-then-hand-edited captions | Sharath Jeppu | Pointed the assistant at a Google Drive folder of curated photos and asked to run `curate-photos.mjs` (dry-run first, then for real). Then asked: "Can captions be auto generated based on the picture and then edited later after it is uploaded?", followed by "yes, build it and run it." After reviewing the AI drafts on an Artifact review page, asked "Can this be made easier where I can make the changes directly on the rendered page?", then reported "The copy is failing. The download button is not working" when the sandbox blocked clipboard/download access. Pasted back full caption rewrites in two rounds, then confirmed a last typo fix ("Members" vs "Member's") and to leave one photo's AI caption as-is. | 44 of 81 source photos were HEIC (unsupported by the curation script) - converted to JPEG via `sips` in a staging folder first, confirming EXIF/GPS survived the conversion. Ran `curate-photos.mjs` for real: uploaded display + thumbnail JPEGs for all 81 photos to the `tvc-photos` R2 bucket and wrote one `src/content/photos/*.md` entry each, all with GPS and camera EXIF intact. Built `scripts/caption-photos.mjs`, which sends each thumbnail to Claude's vision API (`claude-haiku-4-5`, same request pattern as the existing chat widget's Anthropic call) and backfills a draft caption for any entry still carrying its filename-derived placeholder; ran it to draft all 81. Built an interactive Artifact page (embedded thumbnails as base64 so it renders inside the CSP-sandboxed preview) with directly-editable, autosaving captions; when the Artifact sandbox turned out to block both `navigator.clipboard` and anchor-triggered downloads, replaced those with a plain read-only textarea the user selects and copies manually. Applied the user's hand-written captions - real names, places, and stories the AI couldn't have known (e.g. "the Elvin Kere", "Nishant's pool", "the Lambda retreat"-adjacent context) - to 80 of the 81 entries, fixed a "Member's"→"Members" typo and a couple of the user's own typos on request, and left one entry's AI-drafted caption in place per instruction. Updated `ARCHITECTURE.md`: added `caption-photos.mjs` to the offline-curation subgraph, noted the Anthropic API is now called both server-side (`chat.mts`) and from this local script, and split the diagram's "runs locally" styling out from the Cloudflare-specific styling it was previously (incorrectly) sharing. | `b2a333e`, `9a590f2` |
| 2026-07-19 | Rebuild In Pictures: date/camera EXIF filtering, a Grid/Map view, offline curation via Cloudflare R2 | Sharath Jeppu | "I would like to scaffold an alternate design for the 'in pictures' page... A way to allow a user to navigate through the pictures by filtering through dates. This should also support pictures that [have] EXIF data in them. The pages should be able to show where the picture was taken on a map (OSM). The picture should also show meta data on what device was used to take the picture, the shutter speed, Aperture, iso, etc. Check on flickr does this. The uploads to this album will be curated offline. The pictures will be then be uploaded to a storage (recommend the best option for this)." Iterated on a mockup first (dark-mode-in-artifact fixed to match the site's single theme, Upcoming-style highlight rejected in favor of the current layout, Google Photos link-out removed, a second "filter by camera" facet added and put on its own line) before building; recommended Cloudflare R2 for storage over Cloudinary (zero egress, same Cloudflare account already fronting the site), which the user then set up (bucket `tvc-photos`, custom domain `media.tvc.farm`). | Added a `photos` content collection (src, thumbSrc, caption, takenAt, optional gps/camera/exif) and `scripts/curate-photos.mjs`, an offline tool that extracts EXIF/GPS (`exifr`), generates display + thumbnail sizes (`sharp`), uploads both to the `tvc-photos` R2 bucket, and writes one content entry per photo - run locally only, credentials never touch Netlify. Migrated the 114 existing photos into the collection as basic entries (confirmed via exifr none have real EXIF left). Rebuilt `/in-pictures` around a new `PhotoGallery` component: independently-combinable date and camera filter chips, a Grid/Map toggle, and a lightbox with a Flickr-style EXIF panel and a per-photo location map - the map (both the aggregate view and the lightbox's mini map) reuses the Leaflet + OpenStreetMap setup already proven on `/ecosystem`, not a new library. Verified end-to-end with a temporary GPS/EXIF test fixture (reverted before committing), which caught and fixed two real bugs: the EXIF panel and map sidebar are built via `innerHTML` client-side so they didn't inherit Astro's scoped-style attribute (moved those rules to `is:global`), and `fitBounds` was zooming in past OSM's available tiles for a single geotagged photo in this rural area (capped `maxZoom: 15`). Updated README.md and ARCHITECTURE.md for the new collection, script, and R2 as an external service. | `b3169c6`, `06d55ce`, `73ec1c8`, `c6f4bdc` |
| 2026-07-19 | Redesign Events: highlighted Upcoming with countdown, tag/duration filters, timeline view | Sharath Jeppu | "Update the events page to allow for filtering based on tags. Also need a better title to replace 'Upcoming and Past events'. Can we also have a timeline view like we have on the timeline page? The dates are duplicated in each title making some of them too big. Tag them additionally also to indicate if it is a day event or overnight event and if overnight, the number of days." Iterated on a live mockup first: "show me the new layout first" → approved the base concept, then "Keep the page look and feel consistant with the otehr pages. The dark background is incosistant. Need to high[light] upcoming event(s) as a seperate subsection like earlier. Add a countdown timer to it (days)" → "instead of calling it 2-day event, call it Day event and Overnight event" + "with the humber of days in ()" → "looks good." | Retitled the page "Camps, Workshops & Gatherings." Added an optional `endDate` field to the events schema and stripped the redundant date prefix baked into every event title (e.g. "2022: Dec 11/16 - The Lambda Retreat" → "The Lambda Retreat"), backfilling endDate on the 5 events that actually span more than one day - also fixing the Lambda Retreat's `date`, which had been set to the event's end rather than its start like every other event. Added `src/utils/events.ts` (formatDateRange, durationTag, daysUntil, countdownLabel), shared by the listing and detail pages, so multi-day events show a real date range instead of losing the end date, and get a duration tag ("Day event" / "Overnight event (N days)") visually distinct from content tags. Upcoming is now a permanently-visible highlighted section with a live countdown per event; added type/length filter chips built from the real tag data; gave the Past archive a Grid/Timeline toggle, the timeline a vertical line with a node per event. Verified end-to-end in-browser against the real 13-event dataset. | `5762491` |
| 2026-07-19 | Full-site audit for inconsistencies and orphaned elements, then fix all findings | Sharath Jeppu | "check the overall website for inconsistences and orphaned elements and provide a list of recommendaitions" — a list of 18 findings was presented (nav de-listing gaps, breadcrumb/title mismatches, stale docs, an orphaned component and content collection, unreferenced images, duplicate prose, dead schema fields, inconsistent spelling) — followed by "fix everything." | Ran three parallel Explore-agent audits (orphaned files/broken links, nav/routing structure, content/docs consistency), consolidated into a ranked list, then fixed all 18 items across 3 commits: (1) delisted Partners from the top-level Explore nav (same pattern already fixed for Timeline/Design/Community Outreach), added the missing Geography & Weather homepage card, fixed 3 pages' breadcrumb-vs-nav-label mismatches, fixed Community Outreach's detail-page eyebrow and Our Journey/Design's missing OG dimensions and title casing, documented the homepage Engage ladder's intentional 4-of-7 asymmetry, and synced README.md/ARCHITECTURE.md's stale "Newsletter signup"/"Timeline" references; (2) deleted the orphaned `Pending` component, the empty `products` content collection and its dead route, the unused `partners.url` schema field, and 7 unreferenced image files; (3) deduped near-identical membership copy between the homepage and About, surfaced the `events` collection's `tags`/`organizer` fields (previously captured but never rendered) as visible chips/byline, and standardized "Thagatti"/"Thaggatti" village-name spelling site-wide. Verified with a full `astro build` (40 pages, no warnings) and in-browser checks after every stage. | `10f34b4`, `f505c93`, `19f3e1b` |
| 2026-07-19 | Sync ARCHITECTURE.md with the Anthropic key activation and Netlify account move | Sharath Jeppu | "The Anthropic API key was updated yeterday. This is not reflecting in the documentation. Also, the netlify account has been changed from Rajesh's personal account to the one that is owned by contact@tvc.farm. Update all documentation files." | Both facts were already true in production and captured in this changelog's 2026-07-18 05:29 entry, but `ARCHITECTURE.md` — the diagram and status doc, not this log — had never been updated to match, so it still described the chat function as waiting on its API key. Verified current behavior directly against production first (`POST https://tvc.farm/api/chat` now returns real, grounded answers instead of the "not configured yet" fallback), then updated `ARCHITECTURE.md` throughout: the Overview, the Mermaid diagram's `/api/chat` node, the Hosting section, the Visitor's Browser section, and the Current Production Status table now all show the chat assistant as fully live, and note the Netlify project is owned by `contact@tvc.farm` (moved from a personal account on 2026-07-18). Removed the now-resolved "remaining gap" callout. `README.md` wasn't touched — its `ANTHROPIC_API_KEY` mentions are generic setup instructions, not a status claim, so they weren't stale. | `313632a` |
| 2026-07-19 | Give every page a photo hero, matching Our Journey's style; fix the Biodiversity picture | Sharath Jeppu | "The picture in teh hero banner looks nice on the Our Journey page. Can this be made consistant across all the other pages. Also, choose a better picture for biodiversity. The poster does not render well in a landscape orientation." | `PageHero` has always supported two variants: a photo hero (image + scrim + white title text, as used on Our Journey) and a flat "plain" hero with no photo. 13 pages were still on the plain variant — `about`, `contact` and `contact/thanks`, `ecosystem`, `ecosystem/geography`, `ecosystem/partners`, `events`, `in-pictures`, `join`, `resource-centre`, `visit`, `visit/camping`, `visit/day-visit`, and `our-journey/community-outreach`. Gave each a photo, reusing the image the homepage already associates with that destination wherever one existed (e.g. `visit/camping` now uses the same sunflower photo its homepage "Spend the night" card links through to), so the teaser and the landing page always agree — the same pattern Our Journey itself already followed. Also set each page's `BaseLayout` `image`/`imageWidth`/`imageHeight` to match, improving social-sharing previews along the way.<br><br>The old Biodiversity hero source was `events/2024-12-21-3bs-and-1h-biodiversity-walk/hero.jpg` — a portrait (1080×1375) event poster with its title, schedule, and pricing baked into the image as text. Cropped into a wide landscape banner via `object-fit: cover`, most of that text was lost. Replaced it with a real landscape photo (1384×1038) of a forest watering hole, and updated the homepage's Biodiversity thumbnail to match. | `a9d3a14` |
| 2026-07-19 | Nest Timeline, Community Outreach, and Design under Our Journey; fix duplicate content across pages | Sharath Jeppu | Across three follow-up messages: "The Our Journey and Timeline can be collapsed into one section under journey instead of having a separate section for timeline"; "The community outreach section can also be collapsed into this instead appearing seperate. Also, remove the section of what the future holds"; and "There are duplicate content in different sections. for example, resource centre and our journey both lead to the community outreach section. Can you identify such inconsistencies and fix them?" | Timeline and Community Outreach were separate top-level Explore nav items and homepage cards despite being entirely children of Our Journey — and unlike their sibling `/our-journey/design`, neither was even nested in its URL. Moved both to `/our-journey/timeline` and `/our-journey/community-outreach`, delisted them from the top-level Explore nav dropdown and the homepage's Explore grid (each is still reachable via its card on the Journey page itself), and added redirects from the old `/timeline` and `/community-outreach` URLs (including individual outreach post slugs). Removed the empty "What the future holds" `Pending` stub from the Journey page. Extended the same de-listing to "The Design," found to have the identical problem.<br><br>A follow-up audit for duplicate/miscategorized content (prompted by noticing Resource Centre and Our Journey both linking to Community Outreach) found: Resource Centre was fully re-listing every Community Outreach post as its own card grid instead of linking to the now-canonical listing page — replaced with a one-line teaser and link. `visit.astro`'s "What to expect" section carried a near-verbatim copy of the land-layout paragraph that's the whole point of `/our-journey/design` — shortened to a link. The homepage and About page each hardcoded the same "Core principles" list independently, risking drift — extracted both into `src/data/principles.ts` so they render from one source, and added a link from the homepage section to `/about` for the fuller treatment.<br><br>Also found and flagged (not fixed, pending a decision): an orphaned `/ecosystem/products/[slug]` route left over from an earlier "drop Produce" cleanup — its `products` content collection has no files and nothing links to it. | `a72cf77` |
| 2026-07-18 | Add a custom 404 page | Rajesh Thiagarajan | "for the website we do not have a good 404 page. Please design a 404 page that is a bit humorous (something like lost in the wilderness of the farm)." | Added `src/pages/404.astro` — a wooden trail-signpost illustration (inline SVG, hand built from the brand's green/orange/wood palette) with one arm pointing back to the farmhouse and one to the trails, a bird perched on top, and a "404" tag dangling loose off a nail with a gentle swing (respects `prefers-reduced-motion`). Copy leans into the farm/wilderness voice ("You've wandered off the map," a goat-knocked-the-trail-marker joke) with CTAs back to `/` and `/visit/trekking-trails`. Added an opt-in `noindex` prop to `BaseLayout.astro` so crawlers don't index the dead end. Astro emits this as `dist/404.html` at the build root, which Netlify serves automatically as the site's custom error page — verified locally that unmatched routes return a 404 status and that the production build produces the file in the right place. | `e446234` |
| 2026-07-18 | Remove the Trees & Plants stub, move Geography & Weather to its own page | Sharath Jeppu | "In the ecosystem page, delete the section on Trees & Plants since it is already covered in the inaturalist integration. Also move the geography, landscape and weather into a new page. This page will get more subsections later." | Removed the "Trees & Plants" section from `/ecosystem` — it was an unfilled `Pending` placeholder, and the content it was meant to cover (plants, trees) is already live in the iNaturalist-backed biodiversity explorer above it. Moved "Geography, landscape & weather" (the intro text, the monsoon-to-date stat tile, the monthly rainfall chart, and the full rainfall table) to a new page at `/ecosystem/geography` — a new landing point for a section that's expected to grow further subsections over time, rather than staying a subsection wedged into the biodiversity page. `/ecosystem` now links out to it via a teaser card. Added the new page to the shared Explore nav dropdown and site-map data, so it shows up in the nav and gets a correct "Home / Explore / Geography & Weather" breadcrumb automatically. Fixed a bug caught along the way: HTML entities like `&amp;` render correctly in plain template text but leak through literally when passed via a component prop (e.g. `title="Geography &amp; Weather"`) — component props need a literal `&` since Astro escapes those safely on its own. | `177051a` |
| 2026-07-18 | Bigger overlapping timeline photo cards, a click-to-detail lightbox, and a smooth carousel | Sharath Jeppu | "The picture tiles on the timeline page are too small especially on a large screen. Can they be larger and the horizantal scroll be a carousal of overlapping pictures. The user should also be able to click on a picture to see details and then exit easily." Followed by three rounds of refinement: "The animation between the transition of overlapped pictures when is not smooth. Can this be fixed?" and "One small change. For tiles that don't have a picture, have the text rendered instead in the middle of the box." | Reworked the per-year month carousel on `/timeline`. Cards grew from a fixed 300×260px to a fluid `clamp(260px, 42vw, 620px)` × `clamp(220px, 30vw, 440px)`, so they scale up meaningfully on large screens instead of capping out small. Cards now overlap via negative margin into a fanned stack, with the focused card scaling up and brightening (and its neighbors receding) as you scroll — tracked live every animation frame rather than only after scrolling stops, so the effect sweeps continuously instead of popping in late. Clicking any photo opens a new shared lightbox (full-size image, complete caption, prev/next through that year's photos, closable via an X button, clicking outside, or Escape, with focus returned to the card on close); on-card captions are now truncated to a teaser (3 lines) since the full text lives in the lightbox. Cards with no photo for a month get centered text instead of a caption pinned to the bottom, since there's no image to anchor it to.<br><br>Along the way, found that the arrow-button navigation was jumping instantly with no animation at all — native `scrollTo({behavior:'smooth'})` was silently not animating on this track (not just "getting fought" by `scroll-snap-type:mandatory` as previously assumed; verified it does nothing even with snap disabled first). Replaced it with a hand-rolled `requestAnimationFrame` tween that eases the scroll position directly, which can't be silently dropped, and which respects `prefers-reduced-motion`. Verified on desktop and mobile — card sizing, the overlapping/fanning motion, lightbox open/close/prev/next including on a no-photo-adjacent card, keyboard (Enter/Space to open, arrows to navigate, Escape to close), and touch swipe not misfiring the lightbox open. | `518ebdb` |
| 2026-07-18 | Restructure the main nav into Explore/Engage, add breadcrumbs everywhere | Sharath Jeppu | "Can the main nav be restructured based on how the website is now structred? I tend to hit the brower back button after navigating to a page. It would be good to always have a website map to navigate in and out of pages. Propose a solution to solve this" — a proposal was floated (restructure the nav dropdowns to mirror Explore/Engage, add breadcrumbs to every page, and expand the footer into a full sitemap), then "Build all three," followed by feedback after seeing the built footer: "The footer is too big. Not really a fan. Is there a reason why it makes sense to have all the pages listed in the footer?" — resolved by trimming the footer back to just brand/blurb/social/copyright, since the nav dropdowns already list every page and don't need a second copy of the same list. | Added `src/data/site-map.ts` as a single source of truth for the site's Explore/Engage link groups and path-prefix classification, so the nav, breadcrumbs, and any future consumer can't drift out of sync with each other or with the homepage's own Explore/Engage sections. Replaced the nav's old About TVC/Our Journey/Visit TVC/Biodiversity/Events/Contact grouping with two dropdowns — Explore (10 items, two-column) and Engage (7 items) — matching the homepage fork exactly, so the same map is visible (nav is sticky) no matter where you scroll on any page. Added a new `Breadcrumbs` component, wired into `BaseLayout` so every page automatically gets a `Home / Explore\|Engage / {page title}` trail computed from the URL path and the page's own title prop — no per-page wiring needed, and it works on dynamic slug pages (events, partners, community outreach posts) for free. The middle breadcrumb segment links to `/#explore` or `/#engage` on the homepage. Left the footer close to its original shape (brand, blurb, social links, copyright) after the sitemap version proved redundant with the now-comprehensive nav. Along the way, hit and resolved a stale Vite dev-server module cache that was serving an old version of the footer's scoped CSS after the edit — a dev server restart cleared it; not a production concern since a fresh `astro build` doesn't carry over stale in-memory HMR state. Verified nav dropdowns, breadcrumb trails (including a dynamic event page), and the footer on desktop and mobile. | `8cdff81` |
| 2026-07-18 | Redesign the homepage around Explore/Engage, add a Joining the Collective page | Sharath Jeppu | "I would like to redesign the landing page. Here's my thought process. There are different levels of engagement Visit us (day trip) Stay with us (over night stay) Work with us (conduct events, volunteer, etc) Join us (become a member) Also, People would like to explore or engage. So the landing page should start with this. Exploration should link to all the information sections of the website while engagement should lead to all CTA sections of the website. No section should be missed in this process Can you mock up this to be an engaging landing page. Mockup a couple of options to consider" — followed by picking "Concept A" (a two-doors fork into Explore/Engage), then "I would like to add a section related to 'Become a part of the collective'... it is unlikely that our community size will be more than what we are today... The transaction will be directly between the seller and the buyer and the community will only be involved in ensuring that the buyer has a full understanding of the ethos... The process will involve engaging with atleast 2 of the existing members to understand this first," clarified as its own page rather than a homepage section, three copy revisions (softening "expand membership" language, re-articulating the "talk to 2 members" step, and adding that TVC isn't a financial investment — each member values it differently), and finally "Yes, go ahead and build it." | Replaced the homepage with a fork-first structure: a hero, then an "Explore, or engage" choice between two doors. Explore leads to a 9-card grid covering every informational page on the site (About, Our Journey, Timeline, In Pictures, Biodiversity, Community Outreach, Resource Centre, Partners, Events). Engage leads to a 4-rung ladder — Day Visit, Overnight Camping, Work with us (events & volunteering, linking to the existing `/visit` hub), and Join — each rung visually escalating (white → green tint → orange tint) except the Join rung, which is deliberately muted/quiet rather than continuing the escalation, since TVC isn't actively recruiting. Kept the existing principles grid and closing quotes, condensing the former into a lighter strip — this content existed only on the homepage and wasn't duplicated anywhere else on the site. Built a new `/join` page with the honest membership explanation (community size is effectively capped, spot transfers are private transactions directly between members with TVC only ensuring the buyer understands the ethos, TVC isn't a financial investment and means something different to each member, and joining starts with talking to at least 2 current members) alongside real photos of two member families. Replaced the old thin `id="join"` section on `/about` with a short teaser pointing to `/join` instead of straight to `/contact`. Verified locally on desktop and mobile — hero, doors, explore grid, engage ladder, and the new `/join` page all render with real site content and images. | `6c4ed07` |
| 2026-07-18 06:58 | Redesign the Our Journey timeline as a full-bleed scrollytelling page | Sharath Jeppu | "The current deployment of the timeline page is not engaging enough. At first glance most people don't realize that they need to click to see the details. Can you design something like the https://compsych.konpo.co" — followed by a round of comparing rendered options ("wouldn't a full bleed year be better? ... Can you render both for me to compare?"), then "I like the scrollytelling version better, and months as a lighter carousel within a year sounds promising. Can you render and show?", a design tweak pasted in by the user, two more open questions ("How will option 2 render if there is no picture? ... can the timeline markers be more granular than a year"), and finally "lets build it." | Replaced the click-a-year-dot-to-open-a-modal timeline with a full-viewport-per-year page: a fixed sidebar index and progress rail track scroll position, and each year's months now live as a swipeable in-section carousel instead of hidden behind a click — the small icon-dots didn't read as clickable, and nothing hinted at the photos behind them. Reached this design through several rounds of live-rendered Artifact comparisons (photo-tile cards vs. full-bleed takeover, a scrollytelling structure, then months-as-carousel-within-a-year) before building it for real, using the actual per-month photos and narration already in the data — not the mockups' simplified one-photo-per-year placeholders. Months with no photo on file (July 2023, the one real example) get a text-only card instead of an empty image. Removed the old bounded-height page wrapper since this design scrolls naturally; footer and chat widget stay reachable exactly as on every other page, just after a longer scroll. Along the way, fixed a real bug where the carousel's arrow buttons did nothing — `scrollTo({behavior:'smooth'})` was being silently cancelled by `scroll-snap-type:mandatory` on the same element, a known browser conflict — by switching arrow-triggered scrolls to `behavior:'auto'`. Verified locally on desktop and mobile: real photos render per month, the no-photo fallback, carousel arrows/swipe, sidebar navigation, and footer/chat-widget reachability after scrolling through all 10 years. | `9945c29` |
| 2026-07-18 06:12 | Render chat widget replies as formatted Markdown | Sharath Jeppu | "Can the text formatting in the chat bot be richer. It is rendering plain text" — the assistant's replies (bold, bullet/numbered lists, links) were showing as literal `**`/`-`/`[text](url)` syntax instead of formatted content. | `ChatWidget.astro` now parses the assistant's Markdown-style replies into real HTML (bold, bulleted/numbered lists, links) instead of rendering them as plain text; user-typed messages are unaffected. Because this text originates from an LLM response, it's treated as untrusted input: everything is HTML-escaped before any tag is introduced, and link targets are allow-listed to `https://`, `mailto:`, and relative site paths (external links additionally get `target="_blank" rel="noopener noreferrer"`) — verified this blocks `<script>` injection and `javascript:`/`data:` URL schemes. Verified end-to-end on a Netlify draft deploy (isolated from production): bold, both list types, and both internal/external links all rendered correctly against real assistant replies, with no console errors. | `5d7db87` |
| 2026-07-18 05:29 | Activate the chat assistant's AI responses | Sharath Jeppu | "I would like to enable the site chatbot with the anthropic key" — the chat widget and its Netlify Function backend (`chat.mts`) were already built and deployed (see the 2026-07-16 09:32 row below), but had been waiting on an `ANTHROPIC_API_KEY` to actually respond. | Configuration only, no code change. Linked the local repo to the `tvc-farm` Netlify site (ownership had recently moved to the `contact@tvc.farm` account), then set `ANTHROPIC_API_KEY` as a Netlify environment variable (Builds/Functions/Runtime scope, production context) via the Netlify dashboard. An initial key attempt was rejected by Anthropic with `401 invalid x-api-key`; a second, freshly generated key also failed the same way at first because the already-running function hadn't picked up the new value — triggering a fresh `netlify deploy --prod` refreshed it. Verified end-to-end with live requests to both `tvc.farm/api/chat` and the underlying `tvc-farm.netlify.app/api/chat`, confirming grounded replies sourced from the site's own content (e.g. farm description, upcoming events). | _None — Netlify environment-variable/deploy change, not a source change_ |
| 2026-07-17 07:46 | Fix Foraging Day payment link | Rajesh | "The 'Pay & register' link in the current upcoming event in 2026-08-01-foraging-day is wrong. It should be 'https://rzp.io/rzp/Pry5rI8r'." | The event page's "Pay & register" button pointed at an invalid/placeholder Razorpay link (`rzp.io/rzp/ForageTVC`). Replaced with the correct link. Confirmed no other references to the old link existed anywhere in the codebase, and checked the corrected URL is present in the built HTML. | `66614ab` |
| 2026-07-16 18:57 | Replace newsletter signup with "Friends of TVC" WhatsApp signup | Rajesh | "We will not have a news letter. We can aonly collect name & phone number t add them to a whatsapp group 'Friends of TVC'. Need a way to collect thes in place of newsletter sign up in the contact us." | Replaced the `/contact` page's email newsletter form with a "Join Friends of TVC" form collecting name and phone number instead, kept on the same Netlify Forms plumbing (just renamed the form to `friends-of-tvc`, swapped the single email field for name + phone fields). Updated the `/contact/thanks` confirmation copy and `ARCHITECTURE.md`'s mentions of the newsletter form to describe its new purpose (submissions get added to the "Friends of TVC" WhatsApp group by hand — no automated WhatsApp integration). Verified the built form's `name` attribute and hidden `form-name` field still match (required for Netlify's build-time form detection) and checked both pages on desktop and mobile. | `36a98de` |
| 2026-07-16 17:49 | Fix Biodiversity tile alignment | Sharath Jeppu | "There is a tile alignment issue in the Biodiversity page. Align all the tiles to the top." | Found that the 4 observations (out of 109) with no photo on file rendered visibly shorter than their photo-having row neighbors, and their caption ended up vertically centered in the middle of the tile instead of flush at the top — a `<button>`'s content is centered by default even under `display: block`. Fixed by setting `.obs-card` to `display: flex; flex-direction: column; justify-content: flex-start;` in `BiodiversityExplorer.astro`. Verified locally by loading all 109 observations and confirming each of the 4 affected tiles now sits flush with its row's top edge. | `f5fed1f` |
| 2026-07-16 17:33 | Improve the Our Journey timeline modal | Sharath Jeppu | Three related requests in sequence: "In the page our journey so farm, the year and the month gets lost while navigating across the pictures. Can they be made more prominent... Show me locally first"; then "The month and year appear twice which is redundant. Just keep it in the green header and centre it. Also within the same month if there are multiple pictures, there are two separate sliders to scroll. Can this be unified?"; then "The narration also gets lost in the display and users tend not to read it. Is there a way this can be improved?"; and finally "the window is a little small on a large screen. Can this be fixed?" | Made the modal header show a live, centered "Year / Month" readout (with a brief highlight animation on change) as the single source of that information, removing the duplicate text that used to also appear inside each slide. Flattened the nested per-month photo carousel and the outer month track — previously two separate scrollers stacked on each other — into one unified slider, one slide per photo. Moved each photo's narration onto the photo itself as an overlaid caption bar instead of a separate text block above it, so it can't be swiped past unseen. Scaled the modal's size with the viewport (up to 960×840) instead of a flat 640×640 cap that looked small on large screens, leaving small windows and the mobile bottom-sheet layout unaffected. Verified locally at each step (build + browser walkthroughs of month/year transitions, year-boundary crossing, contain-fit images, no-photo months, and mobile width) before committing. | `929659b` |
| 2026-07-16 16:44 | Fix outdated favicon and link-preview logo | Sharath Jeppu | "The logo that appears when we share site links and the favicon are incorrect. They are an older version of orange. Can all these be replaced with the correct logo that we have on the website?" | `favicon-16.png`, `favicon-32.png`, and `apple-touch-icon.png` (also the link-preview fallback image on platforms like WhatsApp) were still generated from the old orange logo, while Nav/Footer/ChatWidget already used the current green logo mark. Regenerated all three, plus two orphaned duplicate-size source files, from the current `tvc-logo-mark.png`. Verified locally: dev server served the new files byte-identical to disk, and the production build (`dist/`) picked up the corrected assets. | `0860bbc` |
| 2026-07-16 16:33 | Fix missing side margins on mobile | Sharath Jeppu | "In the mobile view, the pages seem to have no margins when displaying. It would be good to have a small margin on both sides. Can you fix it?" | Root cause: `.section`'s `padding` shorthand was declared after `.wrapper`'s in `global.css`, so on the ~20 pages combining both classes (`class="section wrapper"`) it silently overwrote `.wrapper`'s side padding entirely. Converted `.section`'s padding to top/bottom-only longhand properties so it no longer clobbers `.wrapper`'s horizontal padding. Verified on mobile and desktop widths before and after. | `c6d6ddf` |
| 2026-07-16 15:18–15:25 | Newsletter signup live, architecture doc synced | Sharath Jeppu | Follow-up "commit and push" for the newsletter form work, plus the standing instruction to keep `ARCHITECTURE.md` in sync with every architecture-relevant change. | Wired the Contact page newsletter form to Netlify Forms (`data-netlify`, honeypot spam field, `/contact/thanks` confirmation page), confirmed it live in production, then updated `ARCHITECTURE.md`'s status table to reflect the newsletter signup going live. | `4019fd6`, `34a4b90` |
| 2026-07-16 15:18 | Rewrite architecture doc for the live site | Sharath Jeppu | "the new website is now live on tvc.farm. Can you review and rewrite the Website architecture document based on the current setup that is live?" | Verified the live production setup directly (Netlify behind Cloudflare, chat function deployed but not yet configured, newsletter form not yet deployed) and rewrote `ARCHITECTURE.md` to precisely match what's actually running in production. | `6f488f7` |
| 2026-07-16 13:23 | Add architecture documentation | Sharath Jeppu | "Can you create an architecture diagram of the current setup of our website? preferably as a word document" followed by "Can you also create a new markdown file with this architecture design and store it on Github? Everytime there is a change, update this file." | Produced a Word document with an embedded architecture diagram (delivered locally, not in the repo) and created `ARCHITECTURE.md` with an equivalent Mermaid diagram covering hosting, data flow, and external services. Added a standing instruction in `AGENTS.md` to keep it updated with every architecture-relevant change. | `52089c1` |
| 2026-07-16 10:36 | Fix WhatsApp/iMessage link previews | Sharath Jeppu | Link previews on WhatsApp/iMessage were showing only the site logo instead of the intended page image. | Root cause: `og:image:width`/`og:image:height` were hardcoded to 1200×630 regardless of the actual image, so WhatsApp's crawler silently discarded the mismatched image and fell back to the site icon. Added per-page `imageWidth`/`imageHeight` props matching each hero image's real dimensions. | `e719a31` |
| 2026-07-16 09:32 | Add a site-wide chat widget | Sharath Jeppu | "I would like to build a chatbot for the website. The chatbot should only respond with answers based on content on the website and nothing else. Use the website logo as the logo for the chatbot." (Chose: Netlify hosting, a custom widget with a serverless LLM call, and the Anthropic Claude API.) | Built `ChatWidget.astro` (a floating widget using the site logo) plus a Netlify Function (`chat.mts`) that calls the Anthropic Claude API server-side, grounded by a build-time script (`build-chat-context.mjs`) that extracts the site's own page text into a corpus so answers only draw from site content. | `56b6051` |
| 2026-07-16 02:48 | Seamless month navigation across years | Sharath Jeppu | "When I slide through the pictures on the time line, I would like the navigation to also move through years seamlessly without having to close the pop up window." | Reworked the timeline's month next/prev button logic so navigating past a year's last or first month crosses seamlessly into the adjacent year without closing the modal (previously the buttons were disabled at each year's boundary, blocking the click entirely). | `d5a82b0` |
| 2026-07-15 22:52 | Remove former members from About page | Sharath Jeppu | "in the about page, remove the member tile for Stataprana and Deb. They are no longer a part of the community." | Removed their entries from the About page member list. | `1489855` |
| 2026-07-15 19:52–22:48 | Build the interactive "Our Journey" timeline | Sharath Jeppu | "Can you create an interactive webpage depicted as a timeline from this content... Default to a collapsed summary by year which the users can then expand to a month timeline... There should be a summary narration for the year when it opens up with pictures by month... Make sure that the render is mobile friendly as well," followed by several rounds of refinement: rewrite the narration as a poem instead of plain prose, fix icon alignment and whitespace, make the year dots more inviting to click, rebuild to exactly match a reference file the user provided, add per-photo captions, structure the data around "eras" to scale for the farm's 25th year, and wire the finished page into the "Our Journey" nav in place of the old duplicate timeline. | Built the era-based `JourneyTimelineStandalone` component (collapsible year view, month drill-down, per-photo captions, monoline icons, poem-based landing copy) as a new `/timeline` page, linked it into the "Our Journey" nav dropdown, removed the old duplicate timeline implementation, and documented it in the README. | `4d02eb6`, `06a9ae1`, `00c8729`, `9bde12e`, `9d93d1b` |
